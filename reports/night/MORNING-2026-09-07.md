# MORNING SHEET — night chain 2026-09-07

**Branch `night/2026-09-07`, off master `fe4e111c`. Nothing is merged. Nothing was minted.**

Rewritten after every piece, so it is true at whatever moment the chain was interrupted.
**Last rewritten: after the DAY chain's piece 2 (SUITE-ENV-SPLIT-1).**

The night chain landed pieces 1–2 and stopped; the day chain of 2026-09-08 continues on the same
branch with its own seven pieces. Both are listed below.

---

## NEEDS HIS WORD

### ★ THE COMEBACKER DEFINITION IS NOT ACHIEVABLE AS STATED — [COMEBACK-DEF-1](COMEBACK-DEF-1.md)

He asked whether his shape is possible. **It is not, within the speed limits he said must stand.**

- The tempo clamp is **+10%** of a racer's own base speed (`racePlanner.js:98-99`, enforced `:891`).
- A racer who **drew P1**, held at rank ~16 until 70%, finishes **8th–15th** — and is already
  **pinned at the ceiling for 23–40% of the climb**. He is clamped for two-fifths of it and still
  does not arrive, so the shape is not near the limit — it is well beyond it.
- **Three things are his to decide, and none of them is mine:** whether the climb may start earlier
  than 70%; whether the clamp may move (he said it may not); or whether "reaches the front" is not
  actually part of the definition.

### ★ "Not every race needs one" — the belief does not hold
**0 of 200 races had zero comebackers** (mean 1.5–2.0 per race). It is structurally possible and it
does not happen.

### ★ "Rank 15–20" describes two different races
Every track allows ≥20 racers, so the shape is expressible everywhere. But at **N=20** rank 15–20 is
the BACK of the field; at **N=40** it is mid-field. **His gap to close** — no scaled version,
percentage or fallback was invented.

### The Holm fairness gate is UNANSWERED
Not measurable on this harness: it is the wrong instrument, and the **shipped** control arm already
fails whole-table χ² on 8 of 10 tracks here. Answering it needs the hold threaded into
`sim-fairness.mjs`, which piece 7(d) owns and which has no test yet.

---

## DONE

### 1 · ONE-HOME-RACE-PARAMS-1 — the race-params derivation gets one home
[report](ONE-HOME-RACE-PARAMS-1.md)

**★ The brief's premise was wrong, and that is the finding.** It said the derivation had been
mirrored **twice**. It had been mirrored **thirteen** times — fourteen sites hold it, established by
searching for the derivation's shape rather than its name, then opening each.

**And the copies were not copies.** The browser derives only when auto-scale is enabled and the racer
type has no `displaySize` override; every harness derives unconditionally. They agree today only
because a default happens to be `true`.

Built `client/src/modules/raceParams.js` (the guarded form, reads no config and no storage).
**8 mirrors deleted, 0 added** — all three sites the brief named, plus the golden path and three
fingerprint instruments. **Six full mirrors remain, each named with its reason**; `sim-fairness.mjs`
is one and piece 9 owns it.

- **All four fingerprints UNMOVED** · golden races PASS · client suite 4630 tests PASS.
- **Sabotage came back GREEN first** — the golden path was itself still a mirror and could not see
  the new module. After converting it, both golden races moved and the check exited 1.
- Noted, not acted on: `engine-reach` reports every changed path "outside the hull", yet sabotaging
  this module moved both golden races. The hull is narrower than "can change how a race comes out".
- **The recompute was NOT built** — that is a separate piece and needs his word.

### DAY 1 · COMEBACK-DEF-1 — the comebacker definition, measured
[report](COMEBACK-DEF-1.md) — the answers are under **NEEDS HIS WORD** above.

Sweep, ran alone: 600 races over 10 tracks, two arms on identical seeds, plus 200 races for the cast
census and 100 for the reach arithmetic. **Fairness for the rest of the field: unharmed** (band-reach
−1.6 to +3.7 pp, up on six tracks of ten); the cost falls on the held racer alone. The temporary
measurement arm was **removed**; world fingerprint unmoved before and after, golden races pass.

### DAY 2 · SUITE-ENV-SPLIT-1 — the unit suite builds a browser it does not need
[report](SUITE-ENV-SPLIT-1.md)

254 files, **environment 503.7 s against tests 211.2 s**. 69 files now opt out of jsdom with
`// @vitest-environment node`; **jsdom stays the default**, so a new test file is safe by
construction. **232.2 s → 172.0 s (−26%), all 4630 tests still pass.**

- Membership decided per file in **two passes that both had to agree** — a transitive static scan,
  then running every candidate in BOTH environments and keeping only identical passing *and skipped*
  counts.
- **★ The sabotage shows why the count check is the real gate**: forcing a `localStorage` test into
  node gives 11 failed and **2 PASSED** — one named "an ABSENT key is not a failure", green because
  the global is gone. Pass/fail alone would have accepted it at 2/13.
- **69, not the briefed 137.** The transitive rule is stricter; 189 s of environment remains in the
  other 184 files, named as an opportunity rather than guessed at.
- `maxWorkers: 4` untouched — measured and owner-agreed.

### 2 · CLIENT-BUILD-VERIFIED-1 — nothing built the client
[report](CLIENT-BUILD-VERIFIED-1.md)

Re-established at source: neither `verify` nor `ci.yml` contained a single `vite build` or
`npm run build`, and `client/eslint.config.js` carries **no import plugin**, so nothing it runs
resolves a module specifier. New `scripts/check-client-build.mjs` builds the client, then runs the
bundle-address audit on what it just built. The declaration is **derived** (`client/`, Vite's root,
minus `client/e2e/`), so a client change selects it and a documentation change does not.

**`audit-bundle-address.mjs` is now wired** — as a *step* of the build guard, not a separate routed
guard, because `verify` runs 14 guards at once and a separate audit could read `client/dist`
mid-write.

- **★ Sabotage (a) is the whole argument**: an import of a name that does not exist leaves **ESLint
  at exit 0** and takes the **build to exit 1** (`[MISSING_EXPORT]`).
- **★ Sabotage (b) did not go red on the first try** — an *unused* baked address is tree-shaken out
  and never reaches the package. Baking it into the live fallback reddened the audit.
- **Cost, corrected**: 2.0 s alone but **20.2 s inside `verify`** under 14-way contention — and
  **0 s of wall clock**, because it finishes far inside a critical path owned by `client-suite`.
- `.github/workflows/ci.yml` **untouched** — that is its own order.
- Side-effect of piece 1: `docs/SHIP-CEREMONY.md`'s generated count moved 116→117 once
  `raceParams.js` became tracked. Regenerated.

---

## RUNNING

*(nothing)*

---

## OPEN — not started

**The DAY chain of 2026-09-08**, seven pieces, piece 1 done:

| # | piece | falls |
|---|---|---|
| 3 | Nine of sixteen races are approach, not proof | 4th to fall |
| 4 | The harness cannot drive the production build | 3rd to fall |
| 5 | What an install still needs, minus his decisions | no |
| 6 | The harness camera is not the browser camera | 2nd to fall |
| 7 | The small leftovers | falls first |

**Fall order if the day runs short: 7, then 6, then 4, then 3.** Pieces 1, 2 and 5 do not fall.



---

## SERVICES, as found and as left

| port | what | state |
|---|---|---|
| 4000 | the API | listening, PID 50700 — **untouched** |
| 4173 | the production build | listening, PID 49220 — **untouched** |
| 5173 | dev | not running — **not started** |

Nothing has been stopped, so nothing has needed restarting.
