{
  "_": [
    "THE ONE HOME for the three current fingerprints. No document carries a copy — not even a",
    "generated one. Other documents REFERENCE this file, or say nothing.",
    "",
    "ONE-TRUTH-1 made 19 copies machine-written and machine-checked. That was the wrong shape: a",
    "generated copy is still a copy, it still has to be kept in step, and a reader who finds one has",
    "no way to know whether it is the truth or a stale rendering of it. ONE-TRUTH-2 (the owner's",
    "order) deleted all 19. Where a document's argument needed the reader to know a fingerprint",
    "EXISTS, it now points here; where it did not, the mention was removed.",
    "",
    "WHERE A CURRENT VALUE MAY STILL APPEAR, and nowhere else:",
    "  - this file",
    "  - docs/TAGS.md — the tag register. A tag's hash is what it was when the tag was cut.",
    "  - reports/ — the lab journal. Append-only by rule: a report records what was true on the day",
    "    it was written and must never be rewritten. Listed as a PREFIX rather than 112 filenames,",
    "    which is a decision: enumerating them would be noise that nobody would maintain, and the",
    "    append-only rule already makes every one of them history by construction.",
    "",
    "TO RE-MINT: run the role's `reproduce` command, put the new value here, and commit. There is",
    "nothing else to update — that is the point.",
    "",
    "WHICH DOCUMENT OWNS WHAT, resolved because four of them implied they owned this:",
    "  - THIS FILE owns the CURRENT VALUES. Nothing else does.",
    "  - docs/SIM.md owns the LINEAGE — which change moved which value to which. Its head points here.",
    "  - reports/parity/REBASELINE.md owns WHAT MOVED each one, and now carries no values.",
    "  - docs/SHIP-CEREMONY.md owns the PROCEDURE for minting."
  ],
  "roles": {
    "world": {
      "value": "b6cfd1daf1756f61",
      "covers": "the RACE: physics, plan, outcome",
      "reproduce": "node scripts/fingerprint-default.mjs",
      "mintedOn": "6b5e5408",
      "mintedBy": "BRAKE-SHIP-1 — THE GAP LEADER BRAKE SHIPS ON, BY THE OWNER'S DECISION OF 2026-09-16, and these four values move with it. The product diff is ONE file: client/src/modules/storage/defaults.js, which flips gapBrakeEnabled false->true and sets the owner's three values (allowance 56 world px, maximum authority 13%, window end 0.97). servoNoiseBlindEnabled stays false and the two must never be on together (BRAKE-JERK-1).\n\nSELECTED BY THE TOOL, NOT BY ME: node scripts/engine-reach.mjs --check with the 46 changed paths passed EXPLICITLY reports 8 of 46 can change the race, and client/src/modules/storage/defaults.js and client/src/modules/raceCore.js are in the declared reach of ALL FOUR roles — world and world-off through fingerprint-default.mjs, camera and render through their own GUARD.reach. scripts/sim-fairness.mjs and scripts/parity/goldenRunner.mjs also changed, which are world's further reach entries.\n\nTHE OFF ARM MOVED TOO, AND IT HAD TO: the OFF world differs from ON only in gapRerollEnabled, and the brake runs in both. A branch that moved ON and left OFF standing would be the signal that something reached one arm and not the other.\n\nWHAT THE RACE DOES NOW, measured on the SHIPPED PATH with no config override of any kind, 300 races over ten tracks: the largest lead falls 244.4 -> 187.5 world px (p90 162.0 -> 127.7) while the MEDIAN barely moves, 87.9 -> 86.3 — the brake works in the tail and leaves the ordinary race alone. Races opening a >124 px gap after 0.95, every one of which is a win for the racer holding it, fall 24 -> 8 of 300. Contested finishes 129 -> 125. 57 of 300 races change winner and 141 are byte-identical. The shipped default reproduces the separately measured 56/13/0.97 arm on 300 of 300 races byte-identically, which is how we know the override and the default are the same object.\n\nFAIRNESS CLEARED IT BEFORE THE MINT: BRAKE-FAIRNESS-2 ran the project's own instrument at two FIXED seeds (12345 and 777) on both arms. No start-row row fails on the new defaults alone at either seed; the one Holm-flagged row, luger-hill 30 s, fails on the brake-off arm too and is pre-existing. The brake was exercised: 63,579 commands on 544,477 calls. That run is SHORT of the pinned 300 races per track (it ran 120) and says so.",
      "date": "2026-09-17",
      "lastVerified": "2026-09-17 — MINTED for the gap brake shipping ON. Measured on branch commit 6b5e5408, one measurement per role, by each role's own reproduce command, on a clean tree with no instrumentation. mintedOn names that branch commit rather than the merge commit, because the merge did not exist when the measurement was taken; the merge carries this tree and the tree sha is the check. Old -> new: b35cf477c09a1116 -> b6cfd1daf1756f61. GOLDEN RACES WERE NOT RE-RECORDED and did not move: check-golden-races.mjs:58 states that every input is pinned in its own fixture, so a change to defaults.js cannot reach them — and the guard passes. 2026-09-14 — RE-MINTED for the night/2026-09-12b merge. Measured on the branch tip after the final commit, one measurement per role, by each role's own reproduce command. Old -> new: 8a1977187e9c99b4 -> b35cf477c09a1116. mintedOn names the MERGE commit 18c682ec; the measurement was taken on branch commit becd1d4f, whose tree the merge carries byte-identically (same tree sha). 2026-09-02 — RE-MINTED by FINGERPRINT-TRACK-DEFAULTS-1, an instrument correction and not a world change. Measured on the branch tip, which is the tree master receives. Controls run and reported in full above: the OLD instrument on the NEW tree returns the value being replaced, the NEW instrument on the OLD tree returns the value being written, and exactly one of the ten per-track hashes moved. CAMERA and RENDER were run in the same pass and are UNMOVED, because they already read each track's own default. 2026-08-25 — RE-MINTED by GARDEN-PATH-DEFAULTS-1 (was dc4647be0f55ebdb). MEASURED, not argued: `npm run verify`'s routing SKIPPED all four fingerprints for this change, because the instruments read `server/data/tracks` (gitignored runtime state) while the shipped change is in `server/seeds/tracks`, so routing cannot connect the two. All four were therefore run by hand. That routing gap is reported rather than worked around. 2026-08-22 — RE-RUN IN FULL for the ENDGAME-LAND-CLEAN-1 ship and UNMOVED. Run rather than argued from a closure statement: defaults.js IS inside this instrument's declared closure and this ship genuinely edits it — it adds two camera keys and ships both ON — so only a measurement can say the race is untouched. 2026-08-21 — RE-RUN IN FULL for the START-ONE-WINDOW-1 ship and UNMOVED. Run rather than argued from a closure statement: defaults.js IS inside this instrument's declared closure (36 files) and this ship genuinely edits it — it RETIRES a camera key and adds another — so only a measurement can say the race is untouched. 2026-08-18 — RE-RUN IN FULL on the ENDGAME-THRESHOLD-095 merge 740f605c and UNMOVED. Run rather than argued from a closure statement: defaults.js IS inside this instrument's declared closure (36 files) because the race's keys and the camera's share one file, so only the CONTENTS of the diff make this a camera-only change and only a measurement can say so. Previously 2026-08-17 — NOT re-run at SHIP-RUNIN-1, and that is the stronger answer rather than a skipped step: walking scripts/lib/routing.mjs closureOf from this instrument's declared reach entries gives 36 files, and NONE of the 17 files the merge puts on master is one of them. The ship's only production file is CameraDirector.js, which is outside this closure and inside CAMERA's and RENDER's. engine-reach was deliberately not leaned on: on a committed merge it compares the working tree and can say nothing about the merge. Previously 2026-08-12 — re-run in full on eea0acf2 (the RUNIN-1 merge) and UNMOVED. Run rather than argued from engine-reach: defaults.js IS in the engine hull and this merge genuinely edits it, and engine-reach --check compares the WORKING TREE, so on a tree whose merge is already committed it reports byte-identical and can say nothing about the merge. The two keys it adds are camera keys, which is why the race is untouched — but that is the conclusion the measurement supports, not the reason to skip it. Previously 2026-08-12 — re-run at CLEAN-STATE-1.",
      "supersededMintedBy": "NIGHT-2026-09-12b — THE RACE MOVED BY DESIGN, and these four values move with it. The branch's product diff is five files: heroCurveGenerator.js (hero casting and the authored curves), racePlanner.js (the trajectory servo, the arrival ceiling and the steering), raceCore.js (the engine step), plus RaceScreen/index.jsx and RaceScreen.css. DIRECTION-AUTHORITY-1 (983d9201) is the shape at the centre of it: a staged comebacker is now HELD to a staging rank and RELEASED at 0.70 to race the last 30% back, replacing a round trip that never fired (0 of 180 races, then 9 of 200). Its own commit message said the world and golden fingerprints WOULD move.\n\nSELECTED BY THE TOOL, NOT BY ME: node scripts/engine-reach.mjs --check with the 63 changed paths passed EXPLICITLY reports 10 of 63 can change the race, and client/src/modules/raceCore.js is in the declared reach of ALL FOUR roles (world and world-off through fingerprint-default.mjs, camera and render through their own GUARD.reach). scripts/sim-fairness.mjs also changed, which is world's second reach entry.",
      "supersededDate": "2026-09-14",
      "supersededOn": "18c682ec",
      "supersededNote": "b35cf477c09a1116 was the value for the race as it ran with the gap brake OFF. It is superseded because the RACE changed by design — the owner turned the brake on — not because the instrument was wrong."
    },
    "camera": {
      "value": "5d91f59b9ada16cc",
      "covers": "the DIRECTOR's decisions: state, phase, anchor, zoom, offsets, camT, targets — INCLUDING the ending the director composes after the last crossing (CAMERA-ENDING-WINDOW-1)",
      "reproduce": "node scripts/camera-fingerprint.mjs --quiet",
      "mintedOn": "6b5e5408",
      "mintedBy": "BRAKE-SHIP-1 — THE GAP LEADER BRAKE SHIPS ON, BY THE OWNER'S DECISION OF 2026-09-16, and these four values move with it. The product diff is ONE file: client/src/modules/storage/defaults.js, which flips gapBrakeEnabled false->true and sets the owner's three values (allowance 56 world px, maximum authority 13%, window end 0.97). servoNoiseBlindEnabled stays false and the two must never be on together (BRAKE-JERK-1).\n\nSELECTED BY THE TOOL, NOT BY ME: node scripts/engine-reach.mjs --check with the 46 changed paths passed EXPLICITLY reports 8 of 46 can change the race, and client/src/modules/storage/defaults.js and client/src/modules/raceCore.js are in the declared reach of ALL FOUR roles — world and world-off through fingerprint-default.mjs, camera and render through their own GUARD.reach. scripts/sim-fairness.mjs and scripts/parity/goldenRunner.mjs also changed, which are world's further reach entries.\n\nTHE OFF ARM MOVED TOO, AND IT HAD TO: the OFF world differs from ON only in gapRerollEnabled, and the brake runs in both. A branch that moved ON and left OFF standing would be the signal that something reached one arm and not the other.\n\nWHAT THE RACE DOES NOW, measured on the SHIPPED PATH with no config override of any kind, 300 races over ten tracks: the largest lead falls 244.4 -> 187.5 world px (p90 162.0 -> 127.7) while the MEDIAN barely moves, 87.9 -> 86.3 — the brake works in the tail and leaves the ordinary race alone. Races opening a >124 px gap after 0.95, every one of which is a win for the racer holding it, fall 24 -> 8 of 300. Contested finishes 129 -> 125. 57 of 300 races change winner and 141 are byte-identical. The shipped default reproduces the separately measured 56/13/0.97 arm on 300 of 300 races byte-identically, which is how we know the override and the default are the same object.\n\nFAIRNESS CLEARED IT BEFORE THE MINT: BRAKE-FAIRNESS-2 ran the project's own instrument at two FIXED seeds (12345 and 777) on both arms. No start-row row fails on the new defaults alone at either seed; the one Holm-flagged row, luger-hill 30 s, fails on the brake-off arm too and is pre-existing. The brake was exercised: 63,579 commands on 544,477 calls. That run is SHORT of the pinned 300 races per track (it ran 120) and says so.",
      "date": "2026-09-17",
      "lastVerified": "2026-09-17 — MINTED for the gap brake shipping ON. Measured on branch commit 6b5e5408, one measurement per role, by each role's own reproduce command, on a clean tree with no instrumentation. mintedOn names that branch commit rather than the merge commit, because the merge did not exist when the measurement was taken; the merge carries this tree and the tree sha is the check. Old -> new: 3df640a42e934312 -> 5d91f59b9ada16cc. GOLDEN RACES WERE NOT RE-RECORDED and did not move: check-golden-races.mjs:58 states that every input is pinned in its own fixture, so a change to defaults.js cannot reach them — and the guard passes. 2026-09-14 — RE-MINTED for the night/2026-09-12b merge. Measured on the branch tip after the final commit, one measurement per role, by each role's own reproduce command. Old -> new: 92ab7120a80af8ed -> 3df640a42e934312. mintedOn names the MERGE commit 18c682ec; the measurement was taken on branch commit becd1d4f, whose tree the merge carries byte-identically (same tree sha). 2026-09-11 - RE-MINTED by MINT-CAMERA-PLAN-1 (was 75aef5cd474c54e5). ★ THE VALUE WAS RE-MEASURED ON THIS TREE BEFORE THE RECORD WAS TOUCHED and reproduced the decided value exactly: camera 92ab7120a80af8ed, render 5e5fdc3fb6656d68. WORLD AND WORLD-OFF WERE RUN IN THE SAME PASS and are byte-identical to their records (8a1977187e9c99b4 / aa09ed97a3a32689), and the golden races PASS - which is what an instrument-only correction must look like, and it was run rather than argued. `mintedOn` was PROVISIONAL and was CORRECTED to the merge commit 1fefc13d after CI went green for it (run 34647849721, event push, conclusion success), per THE SHIP ORDER step 11. 2026-09-08 — RE-MINTED by MINT-CAMERA-1 (was 152cf295c4c9ff54, AIM-ROOM-SHIP-1's value). The value was RE-MEASURED on this tree before the record was touched and reproduced the decided value exactly: `node scripts/camera-fingerprint.mjs` -> 75aef5cd474c54e5, ten of ten tracks contributing FINISHED frames. THE OTHER THREE ROLES WERE RUN IN THE SAME PASS AND ARE UNMOVED, each confirming against the record with its own `--check`: world 8a1977187e9c99b4, world-off aa09ed97a3a32689, render 74946ddbeca517a9. That is what an instrument-only correction must look like, and it was run rather than argued — the branch does touch `RaceScreen/index.jsx` and adds `modules/raceParams.js`, so only a measurement can say the race and the drawing are untouched. `CameraDirector.js` is untouched by the branch, established by diff against the merge-base. RECORDED AND NOT FIXED: `scripts/render-fingerprint.mjs:584` still carries the same `isOutcomePhase: false` literal; measured both ways in this pass, fixing it would move the render record 74946ddbeca517a9 -> 40b2de6fcc5bafd8. That is a second mint and a second decision, and it is deliberately NOT taken here. 2026-09-02 — RE-MINTED by AIM-ROOM-SHIP-1 / AIM-ROOM-COMBINED-1 (was ff3072e95001083c / c5a9aadeaf7a1d07, COMPANY-HEADCOUNT-1's values). Measured on the BRANCH TIP, which IS the tree master receives: the branch was caught up with master first (THE SHIP ORDER step 1), so its tree and the merged tree are the same. WORLD and WORLD-OFF were run in the SAME pass and came back byte-identical to the record — only camera and render differ, which is what a camera-only change must look like, and the ten-track frame counts being identical between arms says the same thing from the other direction. 2026-09-02 — RE-MINTED by COMPANY-HEADCOUNT-1. Confirmed on the MERGED tree at 9328886b BEFORE this record was written, and both matched what was measured on the branch tip exactly (camera ff3072e95001083c, render c5a9aadeaf7a1d07), so nothing moved in the merge itself. WORLD and WORLD-OFF were run in the SAME pass on the same merged tree and came back byte-identical to their records — which is what a camera-only change must look like, and it was run rather than argued. `engine-reach --check` reports both changed paths OUTSIDE the engine hull, and the ten-track frame counts are identical between arms, so the race is untouched by construction and by measurement. 2026-08-26 — RE-MINTED by LEADER-LATERAL-BUILD-1 (was 341060f6's value). Measured on the branch tip, which IS the tree master receives because origin/master was already an ancestor of the branch — THE SHIP ORDER's step 1. WORLD and WORLD-OFF were run in the SAME pass and came back byte-identical to the record; only camera and render differ, which is what a camera-only change must look like. The owner's permission was for THESE TWO VALUES AND NO OTHERS, and the write was done surgically so that neither world hash appears in the diff at all. `mintedOn` was provisional and was corrected to the merge commit 334525df after CI went green for it, per THE SHIP ORDER step 11.",
      "supersededMintedBy": "NIGHT-2026-09-12b — THE RACE MOVED BY DESIGN, and these four values move with it. The branch's product diff is five files: heroCurveGenerator.js (hero casting and the authored curves), racePlanner.js (the trajectory servo, the arrival ceiling and the steering), raceCore.js (the engine step), plus RaceScreen/index.jsx and RaceScreen.css. DIRECTION-AUTHORITY-1 (983d9201) is the shape at the centre of it: a staged comebacker is now HELD to a staging rank and RELEASED at 0.70 to race the last 30% back, replacing a round trip that never fired (0 of 180 races, then 9 of 200). Its own commit message said the world and golden fingerprints WOULD move.\n\nSELECTED BY THE TOOL, NOT BY ME: node scripts/engine-reach.mjs --check with the 63 changed paths passed EXPLICITLY reports 10 of 63 can change the race, and client/src/modules/raceCore.js is in the declared reach of ALL FOUR roles (world and world-off through fingerprint-default.mjs, camera and render through their own GUARD.reach). scripts/sim-fairness.mjs also changed, which is world's second reach entry.\n\n★ THE CAMERA DID NOT CHANGE — THE RACE IT IS POINTED AT DID, and a later reader must not read this movement as a camera change. Measured: client/src/modules/camera/ and client/src/modules/storage/defaults.js are BYTE-IDENTICAL to the master this lands on (git diff origin/master HEAD over those paths returns empty). This role moved through its third reach entry, raceCore.js, alone.",
      "supersededDate": "2026-09-14",
      "supersededOn": "18c682ec",
      "supersededNote": "3df640a42e934312 was the value for the race as it ran with the gap brake OFF. It is superseded because the RACE changed by design — the owner turned the brake on — not because the instrument was wrong."
    },
    "render": {
      "value": "06671c1d13850cd7",
      "covers": "the DRAW CALL SEQUENCE: sprite placement, text, styles, transforms, layer order",
      "reproduce": "node scripts/render-fingerprint.mjs --quiet",
      "mintedOn": "6b5e5408",
      "mintedBy": "BRAKE-SHIP-1 — THE GAP LEADER BRAKE SHIPS ON, BY THE OWNER'S DECISION OF 2026-09-16, and these four values move with it. The product diff is ONE file: client/src/modules/storage/defaults.js, which flips gapBrakeEnabled false->true and sets the owner's three values (allowance 56 world px, maximum authority 13%, window end 0.97). servoNoiseBlindEnabled stays false and the two must never be on together (BRAKE-JERK-1).\n\nSELECTED BY THE TOOL, NOT BY ME: node scripts/engine-reach.mjs --check with the 46 changed paths passed EXPLICITLY reports 8 of 46 can change the race, and client/src/modules/storage/defaults.js and client/src/modules/raceCore.js are in the declared reach of ALL FOUR roles — world and world-off through fingerprint-default.mjs, camera and render through their own GUARD.reach. scripts/sim-fairness.mjs and scripts/parity/goldenRunner.mjs also changed, which are world's further reach entries.\n\nTHE OFF ARM MOVED TOO, AND IT HAD TO: the OFF world differs from ON only in gapRerollEnabled, and the brake runs in both. A branch that moved ON and left OFF standing would be the signal that something reached one arm and not the other.\n\nWHAT THE RACE DOES NOW, measured on the SHIPPED PATH with no config override of any kind, 300 races over ten tracks: the largest lead falls 244.4 -> 187.5 world px (p90 162.0 -> 127.7) while the MEDIAN barely moves, 87.9 -> 86.3 — the brake works in the tail and leaves the ordinary race alone. Races opening a >124 px gap after 0.95, every one of which is a win for the racer holding it, fall 24 -> 8 of 300. Contested finishes 129 -> 125. 57 of 300 races change winner and 141 are byte-identical. The shipped default reproduces the separately measured 56/13/0.97 arm on 300 of 300 races byte-identically, which is how we know the override and the default are the same object.\n\nFAIRNESS CLEARED IT BEFORE THE MINT: BRAKE-FAIRNESS-2 ran the project's own instrument at two FIXED seeds (12345 and 777) on both arms. No start-row row fails on the new defaults alone at either seed; the one Holm-flagged row, luger-hill 30 s, fails on the brake-off arm too and is pre-existing. The brake was exercised: 63,579 commands on 544,477 calls. That run is SHORT of the pinned 300 races per track (it ran 120) and says so.",
      "date": "2026-09-17",
      "lastVerified": "2026-09-17 — MINTED for the gap brake shipping ON. Measured on branch commit 6b5e5408, one measurement per role, by each role's own reproduce command, on a clean tree with no instrumentation. mintedOn names that branch commit rather than the merge commit, because the merge did not exist when the measurement was taken; the merge carries this tree and the tree sha is the check. Old -> new: 6a84085e79535dd6 -> 06671c1d13850cd7. GOLDEN RACES WERE NOT RE-RECORDED and did not move: check-golden-races.mjs:58 states that every input is pinned in its own fixture, so a change to defaults.js cannot reach them — and the guard passes. 2026-09-14 — RE-MINTED for the night/2026-09-12b merge. Measured on the branch tip after the final commit, one measurement per role, by each role's own reproduce command. Old -> new: 5e5fdc3fb6656d68 -> 6a84085e79535dd6. mintedOn names the MERGE commit 18c682ec; the measurement was taken on branch commit becd1d4f, whose tree the merge carries byte-identically (same tree sha). 2026-09-11 - RE-MINTED by MINT-CAMERA-PLAN-1 (was 40b2de6fcc5bafd8). ★ THE VALUE WAS RE-MEASURED ON THIS TREE BEFORE THE RECORD WAS TOUCHED and reproduced the decided value exactly: camera 92ab7120a80af8ed, render 5e5fdc3fb6656d68. WORLD AND WORLD-OFF WERE RUN IN THE SAME PASS and are byte-identical to their records (8a1977187e9c99b4 / aa09ed97a3a32689), and the golden races PASS - which is what an instrument-only correction must look like, and it was run rather than argued. `mintedOn` was PROVISIONAL and was CORRECTED to the merge commit 1fefc13d after CI went green for it (run 34647849721, event push, conclusion success), per THE SHIP ORDER step 11. 2026-09-09 — RE-MINTED by BUILD-RACE-CLOSED-1 (was 74946ddbeca517a9, MINT-RENDER-1's value). The value was RE-MEASURED on this tree before the record was touched and reproduced piece 6's number exactly: `node scripts/render-fingerprint.mjs --quiet` -> 40b2de6fcc5bafd8. THE OTHER THREE ROLES WERE RUN IN THE SAME PASS AND ARE UNMOVED, each confirming against the record with its own `--check`: world 8a1977187e9c99b4, world-off aa09ed97a3a32689, camera 75aef5cd474c54e5 — the camera against its OWN new record from MINT-CAMERA-1, not the value it carried before that mint. Golden races PASS, 2 races, every finishing position and time as recorded. That is what an instrument-only correction must look like, and it was run rather than argued. 2026-09-05 — RE-MINTED by MINT-RENDER-1 (was 733b3f100d6a819f, BOARD-PORTRAIT-FIT-1's value). Re-measured on the CAUGHT-UP branch tip 01ea234c, whose tree is the tree master took at c8d988e4, and it reproduced 74946ddbeca517a9 exactly before anything was written to this record. The other three roles were re-measured in the same pass and are unmoved.",
      "supersededMintedBy": "NIGHT-2026-09-12b — THE RACE MOVED BY DESIGN, and these four values move with it. The branch's product diff is five files: heroCurveGenerator.js (hero casting and the authored curves), racePlanner.js (the trajectory servo, the arrival ceiling and the steering), raceCore.js (the engine step), plus RaceScreen/index.jsx and RaceScreen.css. DIRECTION-AUTHORITY-1 (983d9201) is the shape at the centre of it: a staged comebacker is now HELD to a staging rank and RELEASED at 0.70 to race the last 30% back, replacing a round trip that never fired (0 of 180 races, then 9 of 200). Its own commit message said the world and golden fingerprints WOULD move.\n\nSELECTED BY THE TOOL, NOT BY ME: node scripts/engine-reach.mjs --check with the 63 changed paths passed EXPLICITLY reports 10 of 63 can change the race, and client/src/modules/raceCore.js is in the declared reach of ALL FOUR roles (world and world-off through fingerprint-default.mjs, camera and render through their own GUARD.reach). scripts/sim-fairness.mjs also changed, which is world's second reach entry.\n\n★ THE CAMERA DID NOT CHANGE — THE RACE IT IS POINTED AT DID, and a later reader must not read this movement as a camera change. Measured: client/src/modules/camera/ and client/src/modules/storage/defaults.js are BYTE-IDENTICAL to the master this lands on (git diff origin/master HEAD over those paths returns empty). This role moved through its third reach entry, raceCore.js, alone. The same holds for the draw path: renderRaceFrame.js, frameCameraInputs.js and parity/recordingContext.js are all unchanged.",
      "supersededDate": "2026-09-14",
      "supersededOn": "18c682ec",
      "supersededNote": "6a84085e79535dd6 was the value for the race as it ran with the gap brake OFF. It is superseded because the RACE changed by design — the owner turned the brake on — not because the instrument was wrong."
    },
    "world-off": {
      "value": "744bec11644978bb",
      "covers": "the world with gap-reroll OFF — the pre-feature invariant that proves the shipped change is the only thing that moved",
      "reproduce": "node scripts/fingerprint-default.mjs off --gapRerollEnabled=false",
      "mintedOn": "6b5e5408",
      "mintedBy": "BRAKE-SHIP-1 — THE GAP LEADER BRAKE SHIPS ON, BY THE OWNER'S DECISION OF 2026-09-16, and these four values move with it. The product diff is ONE file: client/src/modules/storage/defaults.js, which flips gapBrakeEnabled false->true and sets the owner's three values (allowance 56 world px, maximum authority 13%, window end 0.97). servoNoiseBlindEnabled stays false and the two must never be on together (BRAKE-JERK-1).\n\nSELECTED BY THE TOOL, NOT BY ME: node scripts/engine-reach.mjs --check with the 46 changed paths passed EXPLICITLY reports 8 of 46 can change the race, and client/src/modules/storage/defaults.js and client/src/modules/raceCore.js are in the declared reach of ALL FOUR roles — world and world-off through fingerprint-default.mjs, camera and render through their own GUARD.reach. scripts/sim-fairness.mjs and scripts/parity/goldenRunner.mjs also changed, which are world's further reach entries.\n\nTHE OFF ARM MOVED TOO, AND IT HAD TO: the OFF world differs from ON only in gapRerollEnabled, and the brake runs in both. A branch that moved ON and left OFF standing would be the signal that something reached one arm and not the other.\n\nWHAT THE RACE DOES NOW, measured on the SHIPPED PATH with no config override of any kind, 300 races over ten tracks: the largest lead falls 244.4 -> 187.5 world px (p90 162.0 -> 127.7) while the MEDIAN barely moves, 87.9 -> 86.3 — the brake works in the tail and leaves the ordinary race alone. Races opening a >124 px gap after 0.95, every one of which is a win for the racer holding it, fall 24 -> 8 of 300. Contested finishes 129 -> 125. 57 of 300 races change winner and 141 are byte-identical. The shipped default reproduces the separately measured 56/13/0.97 arm on 300 of 300 races byte-identically, which is how we know the override and the default are the same object.\n\nFAIRNESS CLEARED IT BEFORE THE MINT: BRAKE-FAIRNESS-2 ran the project's own instrument at two FIXED seeds (12345 and 777) on both arms. No start-row row fails on the new defaults alone at either seed; the one Holm-flagged row, luger-hill 30 s, fails on the brake-off arm too and is pre-existing. The brake was exercised: 63,579 commands on 544,477 calls. That run is SHORT of the pinned 300 races per track (it ran 120) and says so.",
      "date": "2026-09-17",
      "lastVerified": "2026-09-17 — MINTED for the gap brake shipping ON. Measured on branch commit 6b5e5408, one measurement per role, by each role's own reproduce command, on a clean tree with no instrumentation. mintedOn names that branch commit rather than the merge commit, because the merge did not exist when the measurement was taken; the merge carries this tree and the tree sha is the check. Old -> new: 19ccb497041a0dae -> 744bec11644978bb. GOLDEN RACES WERE NOT RE-RECORDED and did not move: check-golden-races.mjs:58 states that every input is pinned in its own fixture, so a change to defaults.js cannot reach them — and the guard passes. 2026-09-14 — RE-MINTED for the night/2026-09-12b merge. Measured on the branch tip after the final commit, one measurement per role, by each role's own reproduce command. Old -> new: aa09ed97a3a32689 -> 19ccb497041a0dae. mintedOn names the MERGE commit 18c682ec; the measurement was taken on branch commit becd1d4f, whose tree the merge carries byte-identically (same tree sha). 2026-09-02 — RE-MINTED by FINGERPRINT-TRACK-DEFAULTS-1, an instrument correction and not a world change. Measured on the branch tip, which is the tree master receives. Controls run and reported in full above: the OLD instrument on the NEW tree returns the value being replaced, the NEW instrument on the OLD tree returns the value being written, and exactly one of the ten per-track hashes moved. CAMERA and RENDER were run in the same pass and are UNMOVED, because they already read each track's own default. 2026-08-25 — RE-MINTED by GARDEN-PATH-DEFAULTS-1 (was 854018ee5d3d83e1). MEASURED, not argued: `npm run verify`'s routing SKIPPED all four fingerprints for this change, because the instruments read `server/data/tracks` (gitignored runtime state) while the shipped change is in `server/seeds/tracks`, so routing cannot connect the two. All four were therefore run by hand. That routing gap is reported rather than worked around. 2026-08-22 — RE-RUN IN FULL for the ENDGAME-LAND-CLEAN-1 ship and UNMOVED, for the same reason as WORLD above. 2026-08-21 — RE-RUN IN FULL for the START-ONE-WINDOW-1 ship and UNMOVED. Run rather than argued from a closure statement: defaults.js IS inside this instrument's declared closure (36 files) and this ship genuinely edits it — it RETIRES a camera key and adds another — so only a measurement can say the race is untouched. 2026-08-18 — RE-RUN IN FULL on the ENDGAME-THRESHOLD-095 merge 740f605c and UNMOVED. Run rather than argued from a closure statement: defaults.js IS inside this instrument's declared closure (36 files) because the race's keys and the camera's share one file, so only the CONTENTS of the diff make this a camera-only change and only a measurement can say so. Previously 2026-08-17 — NOT re-run at SHIP-RUNIN-1, for the same closure reason as the ON world above: this is the same instrument under a flag, so it has the same 36-file closure and the same answer. Previously 2026-08-09 — re-run at SHIP-ROUTING and UNCHANGED, so the pre-feature invariant holds either side of this ship too. SHIP-THREE did not re-run it: nothing in that ship touches the engine (engine-reach clears every changed path except defaults.js, whose only moved key is read by the camera), and the ON world is unchanged, so the OFF arm has no question to answer. Previously re-run at the SHIP-THE-LINE merge and UNCHANGED, so the pre-feature invariant still holds either side of that ship. Previously 2026-08-06 — RESOLVED and VERIFIED by running it. This was UNKNOWN in ONE-TRUTH-1. docs/SIM.md's lineage table names the flag; the first command written here OMITTED THE LABEL argument, argv[2] swallowed the flag as a label, and the run returned the ON value while printing 'default config'. The label is mandatory. With it, this value reproduces exactly. Re-run at RENDER-SAMPLER-CEREMONY on 0308f7b9 and UNCHANGED, measured rather than argued because the ON world was measured too.",
      "supersededMintedBy": "NIGHT-2026-09-12b — THE RACE MOVED BY DESIGN, and these four values move with it. The branch's product diff is five files: heroCurveGenerator.js (hero casting and the authored curves), racePlanner.js (the trajectory servo, the arrival ceiling and the steering), raceCore.js (the engine step), plus RaceScreen/index.jsx and RaceScreen.css. DIRECTION-AUTHORITY-1 (983d9201) is the shape at the centre of it: a staged comebacker is now HELD to a staging rank and RELEASED at 0.70 to race the last 30% back, replacing a round trip that never fired (0 of 180 races, then 9 of 200). Its own commit message said the world and golden fingerprints WOULD move.\n\nSELECTED BY THE TOOL, NOT BY ME: node scripts/engine-reach.mjs --check with the 63 changed paths passed EXPLICITLY reports 10 of 63 can change the race, and client/src/modules/raceCore.js is in the declared reach of ALL FOUR roles (world and world-off through fingerprint-default.mjs, camera and render through their own GUARD.reach). scripts/sim-fairness.mjs also changed, which is world's second reach entry.\n\nTHE OFF ARM MOVED TOO, AND IT HAD TO: the OFF world differs from ON only in gapRerollEnabled, and the engine change runs in both. A branch that moved ON and left OFF standing would be the signal that something reached one arm and not the other.",
      "supersededDate": "2026-09-14",
      "supersededOn": "18c682ec",
      "supersededNote": "19ccb497041a0dae was the value for the race as it ran with the gap brake OFF. It is superseded because the RACE changed by design — the owner turned the brake on — not because the instrument was wrong."
    }
  },
  "historyHomes": [
    "docs/TAGS.md",
    "reports/"
  ],
  "machineExceptions": [],
  "_machineExceptionsNote": [
    "Files that must contain a current fingerprint as a LITERAL because something reads it — a test,",
    "a baseline, a script. Listed BY NAME, never by pattern, each with its reason.",
    "",
    "The list is EMPTY, and that was measured rather than assumed: every tracked file was scanned and",
    "not one test, script or config consumes a fingerprint string. All 19 former copies were prose or",
    "comments meant for a human. If you are about to add one, make it read this file instead."
  ]
}
