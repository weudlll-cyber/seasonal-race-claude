# MORNING 2026-09-24b — the polish night

**Branch `feat/polish-2026-09-24b`, off master `8efc426f`.**

---

## DONE

★★ **THE BACKLOG CATCH-UP FIRST, AND THE DEBT WAS LARGER THAN THE BRIEF SAID** (`78bcd6e8`). The
brief said DELIVERY-BACKUP-1 closed two items and updated neither document. True — but **the
subjects were never in `BACKLOG.md` at all.** NIGHT-2026-09-24 found five delivery gaps and wrote
every one of them only to `OPEN.md`, the short readable page; the owning document was never told. So
this was not a move from PART ONE to PART TWO — PART ONE never had them — it was both halves being
written for the first time. Five subjects closed in PART TWO with the commit that did it, three
still-standing ones opened in PART ONE, and **the owner's rule of 2026-09-23 recorded in
`docs/SHIP-CEREMONY.md`** as a dated fact.

★ **DEPLOYMENT.md's four assumptions were checked one at a time, as instructed. All four are
closed** by `842371e6`: it now says to install dependencies (`npm ci --prefix` ×4), names `openssl`,
names *"Node 20 or newer"* from the declared `engines`, and `export`s `RA_BOOTSTRAP_TOKEN` instead of
prefixing it. **Nothing on that row still stands.**

**PIECE 3 — the small fixes that protect data. All six done, each with tests, each closed in BOTH
documents in the same commit.**

| | what shipped |
|---|---|
| **3(a)** Q-22 | A crash can no longer lose a hand-drawn track. It **offers** the draft rather than restoring silently, does not offer when something is already on screen, and is retired by the existing save signal. 9 tests. ★ Closed as the **new-track half**; the per-track load-mode key is split out as **Q-22b**, not left implied. |
| **3(b)** Q-20b | The server test cleanup survives Ctrl+C. Does the **file half only** (an awaited round-trip may never resolve on a signal) and **re-raises the signal** — a harness that breaks Ctrl+C is worse than one that litters. |
| **3(c)** Q-20c | `.tmp` orphans from an interrupted write are swept **at boot, before anything is served**, so it cannot race a live write. Removes only `*.tmp`; non-fatal in every direction. 6 tests. |
| **3(d)** Q-24 | A default track cannot be un-defaulted. ★ **The behaviour was already correct — a test gap, not a defect**, and reported as such. Sabotaging `tracks.js:542` turns both tests red. |
| **3(e)** Q-25 | A save that lands while its background fails now **says which half failed**, and that the geometry is safe. Deliberately not "Saved ✓". |
| **3(f)** Q-26 | An out-of-range min/max is refused **out loud**. Reuses the Dev Screen's existing amber warning style. 3 tests. |

---

## RUNNING

**PIECE 1 — is `TrackEditor.effects.test.jsx` really flaky?** 20 full client-suite runs against an
unchanged tree, counting. **7 of 20 done, 7 GREEN, the suspect file has not failed once.** Each run
is 199–250 s. ★ No fix is being attempted: a flake whose rate nobody has measured cannot be fixed on
purpose.

---

## OPEN

- **PIECE 2** — what 51.6 MB of backgrounds cost at first paint. Measure only.
- **PIECE 4** — the tidying: worktree-stub helper, one shared slider, tooltips for the six sections
  that have none, a hint when a track is saved with no background.
- **PIECE 5** — the migration ledger. ★ Second to fall under the budget rule.
- **PIECE 6** — the racer-config move, 80 files. ★ First to fall.
- **PIECE 7** — the report, the consistency pass, the merge.

---

## NEEDS HIS WORD

Nothing new. ★ One thing is worth his eye at some point but is **not** being built: **Q-22b**, the
load-mode half of the track draft. Editing an existing track is still not drafted, and closing it
needs a per-track key — a second storage shape rather than a widening of the first.
