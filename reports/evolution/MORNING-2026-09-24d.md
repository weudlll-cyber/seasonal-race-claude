# MORNING 2026-09-24d — resume and finish

**Branch `feat/night-2026-09-24c`, continued. Heartbeat: a line here at least every 30 minutes.**

---

## DONE

### PIECE 3(b), 3(c), 4, 5, 6 — the six that were left ✅

Six pieces from the brief closed. Full client suite green (268 files, 4761 tests) at every step;
four fingerprints unmoved throughout (checked with `check-fingerprints --mint`). See NIGHT-2026-
09-24D.md for the item-by-item report.

**PIECE 7 was deferred** as the brief permits when time runs short — the algorithm is
unit-tested already; the live-race measurement instrument would take substantial time to build
with its sabotage test.

### PIECE 2 — same-origin by default, his 4173 flow proven safe ✅

The server marks **every** page it serves; an empty `apiBaseUrl` positively states same-origin.
★ **What was broken:** an un-configured server was indistinguishable from any other static server,
so the standalone image's client fell back to `localhost:4000` — *the recipient's own machine*.
★ **The escape hatch is structural, not a flag:** 4173 is a plain `node:http` static server that
never gets a marker. **Proven by running both** — 4173 has no marker and still bakes
`localhost:4000`; our server with no `RA_PUBLIC_ORIGIN` returns `{"apiBaseUrl":""}` and answers
`/api/health` on that same origin.

### PIECE 1 — the self-contained image (his decision of 2026-09-23) ✅

`server/Dockerfile` has a `client-build` stage; `additional_contexts` is gone.
**328 s → 336 s, +2.4%** on a cold cache — the proposal feared a whole front-end install and it is
small against the Alpine native-module compile that already dominated. **Proven by running it**: no
mounts, Up (healthy), `/api/health` 200, and the served `index.html` referenced an asset that
returned 934,343 bytes.
★ **Two findings.** The client is **not self-contained within `client/`** — four modules import up
into `shared/`, and a flat copy died with six UNRESOLVED_IMPORT errors. And
`check-container-paths` had a **stage blind spot**: it read build-stage COPYs as runtime copies.
Fixed by teaching it that only the final stage counts, rather than declaring a divergence that would
have stated something false; sabotage-tested both ways.

### PIECE 0 — what happened in the eight hours, said plainly

★★ **Nothing stalled, nothing blocked, and nothing asked a question. The turn ended.** The last
push at 23:48 (`c008f21f`) was complete and the work stopped there; the eight hours after it contain
no work at all, rather than lost work.

**Established rather than assumed:**

- `git status` — clean apart from `.claude/settings.json`, which is always excluded.
- `HEAD == origin/feat/night-2026-09-24c` at `c008f21f`. **No uncommitted or unpushed work.**
- Five `node` processes were alive, and **not one of them was a RaceArena measurement**: PIDs
  32092/31360 are the API dev server (`dev:once`, port 4000, started 23.09 14:13), 26084/35308 are
  the vite client (port 5173, started 23.09 15:43), and 26896 is **Adobe Creative Cloud**.

★ There is no external cause to report. The brief asks what stopped, blocked or asked a question;
the truthful answer is that none of those happened.

---

## RUNNING

*(heartbeat lines appended below, newest last)*

- **08:00** — PIECE 0 done and pushed. Starting PIECE 8 (editor-flake bounding) in the background;
  PIECE 1 in the foreground. PIECE 7 not started yet — it needs an instrument built first.
- **08:35** — Two measured stamps went stale from last night's 3(d); **re-measured, both identical
  to the digit**, re-stamped. Pushed `79fd1398`.
- **09:20** — **PIECE 1 DONE**, pushed `1bee895a`. The image builds its own client: cold-cache
  328 s → 336 s (**+2.4%**, far below the feared penalty), run with no mounts, healthy, serving a
  934 KB bundle it built itself. Two findings: the client is **not** self-contained within `client/`
  (four modules import up into `shared/`), and `check-container-paths` had a **stage blind spot** —
  fixed and sabotage-tested rather than declared around.
- **09:20** — PIECE 8 running: 5 of 25 further suite runs, all GREEN, suspect file clean.
- **10:05** — **PIECE 2 DONE**, pushed `86733290`. **Both of his delivery decisions are now built
  and proven by RUNNING them.** Same-origin marker on every page our server serves; his 4173 flow
  proven untouched (no marker, `localhost:4000` still baked in, API on 4000 answering). One test
  rewritten rather than deleted, because it asserted the behaviour the decision reverses.
  Next: PIECE 3 (the three small builds).
- **18:20** — **PIECE 3(b) DONE**, pushed `77c6d6f7`. One shared `RangeSlider` component; the
  BrandingProfiles (×2) and SurfaceClassManager (×1) slider markups collapsed into it. Full client
  suite green (268 files, 4761 tests); four fingerprints unmoved. ★ The brief said "three sections"
  — by count it is three INSTANCES across TWO sections; the most conservative reading is that the
  duplication to remove is the actually-shared markup. Recorded in the commit and here.
  Starting PIECE 3(c) — tooltips for six sections.
- **18:35** — **PIECE 3(c) DONE**, pushed `ff4ee561`. Tooltips on the six named sections
  (SystemSettings, RaceTuning, ConfigExport, SurfaceClassManager, UserManagement, ChangePassword)
  reusing the existing `InfoTooltip`. **A test found a defect**: three exact-string `getByLabelText`
  matches broke when the label absorbed the tooltip icon; fixed by anchored regex, not by
  weakening. Full client suite green. Starting PIECE 4 — the migration ledger.
- **18:50** — **PIECE 4 DONE**, pushed `6a379b1d`. `scripts/migrate.mjs` + eight-test
  `migrate.test.mjs`. The runner refuses to re-run a recorded id; the SABOTAGE test proves that
  guard is not decoration (no-op writer → double-run detected, runs=2 instead of 1). The
  observable-state fallback backfills the ledger without re-running on an instance that migrated
  before this ship — verified against `server/data/`: `--status` reads
  `teams-1: backfilled-from-state`. `docs/DEPLOYMENT.md` step 6 now names one command.
  Starting PIECE 5 — the racer config move.
- **19:15** — **PIECE 5 DONE**, pushed `d6df5184`. 80 files moved out of the engine tree
  (`client/src/modules/racer-types/` → `client/src/racer-types/`), 29 external importers and 13
  scripts re-pathed. Full client suite green (268 / 4761). Four fingerprints unmoved. RACE HULL
  stayed at 202 files. Ceremony and SIM.md regenerated. **A defect found and fixed**: prettier
  collapsed a multi-line import in `RaceScreen/index.jsx` (4 lines), which broke six
  line-number citations in `docs/FORCE-MAP.md` and one in `docs/branding.md`; each cite shifted
  by −4 to match reality. Starting PIECE 6 — the frame-camera guard.
- **19:50** — **PIECE 6 DONE**, pushed `ee50c9f8`. `scripts/check-frame-camera-inputs.mjs` +
  five-test `check-frame-camera-inputs.test.mjs`. Guard scans for `anchorRacerIndex:` /
  `comebackLockedRacerIndex:` / `runInArrived:` as object keys outside the ONE HOME. SABOTAGE
  test proves the guard fires on a hand-written literal. Ran on the tree: 540 files, no
  findings — as expected on the tree the 2026-09-05 repair is already applied to. Bundled
  a re-stamp of two `MEASURED:` stamps in `CAMERA_DIRECTOR.md` and `ENDING-PHASES.md` that
  PIECE 5's pure move made stale (numbers unchanged by construction).
- **20:05** — **PIECE 7 DEFERRED** (first-to-fall by the brief when time runs short). The
  algorithm the guarantee runs is already unit-tested at
  `client/src/modules/camera/companyHeadcount.test.js`: a strung-out field with `minVisible=5`
  gets a zoom ceiling that places 5 racers in shot. What the brief asks for is a LIVE
  measurement — drive races on quiet, sample the camera's actual zoom, count racers in the
  90% frame — and quantify how often the guarantee binds and whether OTHER camera constraints
  outrun it. That instrument needs a headless CameraDirector loop and the sabotage test on
  top; building both cleanly at 20:00 with PIECE 9 still ahead is not a good trade. The item
  stays in BACKLOG PART ONE under "needs measuring first" and is called out at the top of the
  NIGHT report. Moving to PIECE 9.

---

## OPEN

★ **This section is REGENERATED from the piece list every time the sheet is touched, never appended
to** — the rule is now written down in [docs/NIGHT-RUN.md](../../docs/NIGHT-RUN.md), "HOW A NIGHT RUN
REPORTS", rule 5. It failed that way once tonight: it still read *"everything from PIECE 1 onward"*
after six of those pieces had landed and been pushed. A heading that was true when it was written and
false when it is read is worse than no heading.

**Regenerated 2026-09-24. Still open, in full:**

- **PIECE 7 — the company guarantee on a spread-out field.** **Dropped under the budget rule** — the brief
  named it as the first to fall when time runs short. **Nothing was built for it.** The
  algorithm is already unit-tested at `client/src/modules/camera/companyHeadcount.test.js`; what is
  missing is the LIVE measurement — drive races, sample the camera's actual zoom, count racers in the
  90% frame — and that needs a headless CameraDirector loop plus its sabotage test. The item sits in
  `docs/BACKLOG.md` PART ONE under "needs measuring first". The deferral note is in RUNNING at 20:05.

**That is the entire open list.** Every other piece of this brief — 0, 1, 2, 3(b), 3(c), 4, 5, 6 — is
done and pushed, and PIECE 9 (the ceremony) closed with the merge below.

### The merge is NO LONGER open — it was authorised and performed

★★ **THE RED WAS PROVEN PRE-EXISTING, AND HE THEN AUTHORISED THE MERGE EXPLICITLY.** The night run
held the merge because `verify --premerge` reported `viewer-invariants` FAIL — 17 crossing violations
— and refused to merge on a *suspicion* that it was pre-existing. It is. The gate guard,
`node scripts/viewer-invariants.mjs --gate`, was run on each tree by itself:

| tree | result |
|---|---|
| `feat/night-2026-09-24c` | 17 crossing violations, 1 of 2 races, 238 s |
| **`master` (`c38ad11c`), the control** | **17 crossing violations, 1 of 2 races, 191 s** |

**The same shape down to the figures.** It is INVARIANT 6, *"the winner's crossing is framed on the
winner"* — all 17 on **`city-circuit` seed 9**, shipped arm, worst frame 6600 at PHOTO_FINISH with
the winner at (0.388, 0.112), outside the subject's inner 0.7. **`space-sprint` seed 9 contributes
zero.** All seven per-invariant counters are 0 on both trees.

**Merged as `ea99bd28`** (`--no-ff`), master pushed, the branch deleted at origin. ★ The red did not
disappear by being measured: it is now a **tracked row** in `docs/BACKLOG.md` PART ONE, mirrored in
`docs/OPEN.md`, with its seeds and its count — **not a merge blocker, and not fixed.** His eye is
owed on it. **Do not "fix" it before he has looked.** The four fingerprints are unmoved, so the
camera behaviour is byte-identical either way.

---

## NEEDS HIS WORD

★★★ **THE MERGE IS DONE — this block's question is CLOSED, and what remains is his EYE, not his
decision.** This section asked him to confirm whether the `viewer-invariants` red was the
pre-existing state. It was answered by measurement instead of by asking: the same guard, run on
master by itself, gives the identical 17 violations. He then authorised the merge explicitly, and
it was performed — **`ea99bd28`**. What is still owed is his eye on the framing itself, on
`city-circuit` seed 9 at PHOTO_FINISH; that is carried as a tracked row in `docs/BACKLOG.md`
PART ONE, not here. The fingerprints are unmoved through the entire run (4/4 re-minted, 0 stray).

---

★★★ **FIRST, AND IT MUST NOT BE BUILT AROUND: is a PAIR at the front too often?** He ruled on
2026-09-24 that a pair is acceptable **if it is not too often**, and has not yet judged the measured
frequency. **Nothing in this night depends on that ruling and nothing invents a threshold.**

| | measured |
|---|---|
| **PAIR breakaways** | **quiet 7 of 300 = 2.3%** · **wild 2 of 300 = 0.7%** |
| **SOLO breakaways** — the picture he already objects to | **quiet 3 of 300 = 1.0%** · **wild 1 of 300 = 0.3%** |

**The four solo cases, for his eye:** `dirt-oval` quick-test seed **20** @ **205 px** (the worst),
`ice-track` seed 10 @ 163, `ice-track` seed 23 @ 159, and at `wild` `dirt-oval` seed 3 @ 166.
**The nine pair cases:** quiet — `ice-track` 20 @ 214, `space-sprint` 27 @ 189, `dirt-oval` 24 @ 184,
`searound` 19 @ 175, `searound` 24 @ 172, `space-sprint` 21 @ 167, `luger-hill` 20 @ 166; wild —
`dirt-oval` 9 @ 208, `garden-path` 13 @ 202.
