# Morning sheet

**Owns:** where the night chain stands, right now. Rewritten after every piece, not at the end.
Whoever reads this at 7 a.m. should not have to open a single report to know where things are.

**Last rewritten:** 2026-08-27, after piece 5.
**Master:** `9e6dfc97` at the start of the chain. Origin carried master alone.

---

## NEEDS YOUR WORD

**An eye-test, on master, two minutes.** *(piece 5 established it)*
Start a NORMAL race — not Quick Test — with the seed field empty. Does the drawn seed appear where
you expect, and read as a number you could type back? Then close the tab, reopen, and check it is
still there. **What it blocks:** the last open half of D23. The build itself landed on master
(`7a3942fa`) and nothing was lost; only your eye is owed.

---

## DONE — merged to master

- **4 — the ship order's twelfth step** (`a741a19f`). THE SHIP ORDER now ends by clearing branches at
  origin, and carries THE CONTAINMENT CHECK beside it: containment is a TREE question, because the
  commit-level check passed a branch whose tree held a file master lacked.

---

## RUNNING

| piece | what it is measuring | where |
| --- | --- | --- |
| **1 — client-suite crowding** | which resource runs out, whether the failure rate depends on load, whether the server-suite remedy is the right shape | worktree `c:/tmp/ra-p1`, branch `night/p1-crowding` |
| **2 — along-track residual** | where the 830 unreachable frames are, how much width would fit them, what that width costs the shot you just accepted | worktree `c:/tmp/ra-p2`, branch `night/p2-residual` |
| **3 — anchor-versus-centre gap** | on how many frames the 132 px input gap changes the guarantee's ANSWER rather than its arithmetic | worktree `c:/tmp/ra-p3`, branch `night/p3-anchor-gap` |

All three are measurement only. None of them builds anything or moves a default.

---

## NOT STARTED

- **6** — fold `ROADMAP.md` into `BACKLOG.md`, redirect the old path, fix every link.
- **7** — garden-path icon and description still say snail; the default racer is the beetle.
- **8** — the harness camera seed follows the browser. Last, because it changes what every
  measurement measures.

---

## AT THE GATE

- **5 — the backlog says things that are no longer true.** Six entries checked AT SOURCE. Three
  closed with their evidence kept (merge gate, run-in hard admit, pan stale zoom); two re-verified as
  correctly still open (garden-path silent zero, contention watch); **one was half-and-half and is
  the reason source beat reports** — the arbiter's routing hole is closed, but its ADVISORY still
  tells you a seed file "cannot reach the engine at all" when editing it moved all four
  fingerprints.
