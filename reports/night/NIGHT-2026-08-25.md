# NIGHT-2026-08-25 — the morning sheet

**Started 2026-08-26 01:10.** Base: `master` at `38a669cb`. Updated after every piece; this file is
its own commit on master each time, so the state here is always what master actually holds.

**How to read this:** every piece lands on master finished, or does not land at all. Stopping me at
any point costs at most the one piece running.

---

## RUNNING NOW

**Nothing. The chain is complete — all eight pieces merged, pushed, branches deleted.**

**Your first stop this morning:** [docs/BACKLOG.md](../../docs/BACKLOG.md), the section *"The night
of 2026-08-25 — everything established, in one place"*. Thirteen findings, each saying what it needs.
**Six of them need only your word** — they are also the four questions at the foot of this sheet.

---

## DONE

**PIECE 8 — one page for you: everything found today.**
[OWNER-FINDINGS-2026-08-25.md](../evolution/OWNER-FINDINGS-2026-08-25.md) + a new dated section at the
head of [docs/BACKLOG.md](../../docs/BACKLOG.md) PART ONE. Collection only — no work proposed.
**Thirteen findings, each with what it is, what establishes it, and what it needs: MEASURING, BUILDING,
or ONLY YOUR WORD.** Six need only your word; three need building; one needs measuring when you want
it; three are context.
**One correction in place:** the older backlog entry said a seed is one of six inputs. It is nine.
**Four of the thirteen turn out to be one defect in four costumes** — a guarantee with more than one
owner, where the copy that stopped being true was not the copy anyone read.

**PIECE 7 — the render fingerprint is blind to the run-in.**
[RENDER-FINGERPRINT-BLIND-1.md](../evolution/RENDER-FINGERPRINT-BLIND-1.md), merged, branch deleted.
**The instrument kept the bug the game had fixed.** `render-fingerprint.mjs:445` builds the frame's
camera object as the same hand-written three-field literal FRAME-INPUTS-1 was created to delete,
leaving `anchorRacerIndex` and `runInArrived` **undefined inside it**.
**So LABEL-FOCUS-1 has never been exercised, and RUNIN-NAMES-1's whole visible change is a state the
instrument cannot enter** — its unmoved verdict for that feature was empty, not reassuring.
A repair is one line and **a mint that needs your word**. The guard that should have caught it checks
the list against the renderer, never a caller against the list — **that half can be closed without you.**

**PIECE 6 — the arbiter that says a track change cannot reach the engine.**
[ENGINE-REACH-DATA-1.md](../evolution/ENGINE-REACH-DATA-1.md), merged, branch deleted.
**A wrong QUESTION, not a wrong answer.** The arbiter computes `raceCore`'s import closure correctly;
`verify` reads that as "can this change alter a race". **I put three paths to it and two verdicts were
wrong** — `racerNames.js` and `racer-types/beetle.js` both return *"cannot reach the engine at all"*
and both change races. **`racerNames.js` is inside the directory the walk is rooted in**, so the
boundary is code-versus-data, not client-versus-server.
Smallest fix: a `reachData` list on the declaration FP-HULL-1 already built. **Cost: 3.4% of commits,
seven a month — and some of those ought to have minted.**

**PIECE 5 — garden-path wears the beetle it races.** **BUILT AND MERGED** —
[GARDEN-PATH-BEETLE-SKIN-1.md](../evolution/GARDEN-PATH-BEETLE-SKIN-1.md). One file, two fields:
🐌 → 🪲 and *crawl* → *scuttle*. Your sentence is otherwise untouched — one verb moved.
**YOU WILL NOT SEE IT ON YOUR MACHINE.** `seedRuntime.js:36` copies a seed only where no file exists,
so this is new installations only. **I did not hand-edit your live record** — that is what made
yesterday's change look delivered when it was not.
**No fingerprint moves** — icon and description are presentation and nothing hashes them.
**The gate ran the server suite and it PASSED (33 s). No bcrypt timeout was forgiven; none occurred.**

**PIECE 4 — the harness runs a camera the product cannot produce.**
[HARNESS-CAMERA-SEED-1.md](../evolution/HARNESS-CAMERA-SEED-1.md), merged, branch deleted.
**43 of 53 callers take the constant; 19 instruments make picture claims that are void.** That is the
whole recent camera line — the hit list, the axis slicing, all three membership reports.
**Your premise was wrong in our favour: the fingerprints do NOT read this default.** They carry a
private copy of the same number, so changing it moves no gate. **The obstacle is the lab journal** —
nineteen instruments' tables would stop matching their tools, and reports are append-only.
**How wrong: re-deriving the hit list gave 30 races, not 26 minus 6 — a DIFFERENT POPULATION.** Your
twelve survive re-measurement.

**PIECE 3 — the seed does not pin a race.** [RACE-IDENTITY-1.md](../evolution/RACE-IDENTITY-1.md),
merged, branch deleted. Design only.
**Nine inputs decide a race; exactly one travels with the seed.** Two are worse than absent —
`raceActionStage` and the world config are **stored host preferences**, so the same seed on two
machines is two races.
**Your Quick Test belief is right, under two conditions that are invisible on screen:** the list comes
from the head of a roster in fixed order with no shuffle — but the roster *selector* picks among
three, and **any real player in the lobby re-indexes the whole field** (he takes a head position and
is filtered out of the fill). **Order is physics:** `stablePairBit` hashes the name and the index
decides who yields.
**The duration is a RESULT, and it is the number the HUD shows** — typing it back feeds it as a
different input and silently produces a different race.

**PIECE 2 — the loud-failure rule for measurement harnesses.**
[HARNESS-LOUD-ZERO-1.md](../evolution/HARNESS-LOUD-ZERO-1.md), merged, branch deleted. Design only.
**56 files import the driver, 44 call `runRace`, and exactly ONE reads its return** — the driver's own
test. `runRace` ends on three different conditions and returns one indistinguishable value. **38 of 44
harnesses have no failure path at all.**
**The case this piece exists for has healed by accident:** garden-path now completes **20/20** because
GARDEN-PATH-DEFAULTS-1 made it a beetle — the harness's hardcoded 2 laps never moved, the racer got
faster. **The silence was never fixed; the symptom moved.** Revert that default and 360 silent
discards come back.
Design: put the loudness in `runRace` — record why it stopped, throw on the ceiling unless the caller
says otherwise. **Cost measured: 0 of 1,140 cells on today's master.**

**PIECE 1 — why the merge gate stopped gating.** [GATE-RED-1.md](../evolution/GATE-RED-1.md), merged
`3f833961`, branch deleted, confirmed at origin. Diagnosis only — no timeout raised, no test skipped,
no cost factor touched.
**Your question was the right one and the answer is: they did not get slower, they got crowded.**
`20868394` (2026-08-18, `fix(TEST-ACCOUNTS-1)`) dropped `--no-file-parallelism` from the server test
script. It was right to — that flag was serialisation standing in for isolation, and that commit made
isolation real. **It measured correctness over eight runs and never measured wall clock.**
bcrypt cost 12 costs **247 ms a call** and **was never raised** (one commit in its whole history). The
worst test with no timeout of its own runs **1,006 ms alone** and **up to 4,979 ms at fourteen
workers**, against a 5,000 ms limit — **21 ms of margin**. Serial puts **zero of 673 tests over 2.5 s**.
**CI is green because a 2-core runner cannot crowd the suite — the green is honest, not hollow.**
**And the reason the GATE rather than the suite is where it shows:** `verify.mjs:246` and `ci.yml:184`
both still assert that flag exists, and **`verify.mjs` schedules on it** — it runs the server suite
non-exclusively beside the fingerprint jobs because it believes the suite is single-worker.

**Before the chain, and already on master:** RUNIN-CHANCE-SET-1 (`38a669cb`) — the sweep that had to
finish before this chain could start. Its findings feed PIECE 8.

---

## LEFT — nothing

**All eight pieces are done, merged, pushed, and their branches deleted.**

---

## NEEDS HIS WORD — started none of these

Three forks the chain explicitly excludes, each with the single question that unblocks it.

**1. The run-in's admit — chance-based, eased, or neither?**
> RUNIN-CHANCE-SET-1 measured it: a chance-based membership removes 13 of the 30 worst width steps,
> holds all twelve of your races, and costs no width — but on seed 13, the race you watched, it moves
> the cut from 0.15 s before the line to 73% through the closing move rather than removing it. The
> membership buys a median 2.28 s of warning and the width spends none of it.
>
> **THE QUESTION: should the width be allowed to ease onto a new member over ~1.25 s, accepting that
> he is not fully guaranteed while it does — or must a racer who is level be framed the same frame?**

**2. Undelivered track changes.**
> Editing a shipped track seed changes nothing any existing installation can see, and there is no
> supported mechanism that delivers it. Your garden-path beetle is the live case. PIECE 5 changes the
> icon and description in the one place that owns them and will state plainly whether your own
> installation would ever see it.
>
> **THE QUESTION: should a shipped-data change be deliverable to an existing installation at all, or
> is "new installs only" the intended behaviour?**

**3. The server suite's parallelism — restore the flag, or forgive the timeouts?**
> PIECE 1 measured it: putting `--no-file-parallelism` back takes every one of 673 tests under 2.5 s.
> But `20868394` dropped it deliberately as part of making test isolation real, and restoring it must
> not restore that reasoning — it would be serialisation for SPEED, with isolation still carried by
> the per-file stores. One thing is unestablished and blocks it: `verify.mjs` claims the files share a
> real sqlite session store. If that is still true, parallelism is a correctness bug too.
>
> **THE QUESTION: should the server suite go back to running its files one at a time — or should the
> gate learn to report a timeout-only failure as INCONCLUSIVE rather than as a pass or a fail?**

**4. The harness's camera-seed default.**
> Since your decision of 2026-08-23 the browser derives the camera seed from the race seed, while the
> shared harness still defaults to a constant no race can produce. Every harness claim about what a
> particular race LOOKS LIKE is measured on a camera the product cannot make. The fingerprints pin
> that constant on purpose, so changing the default is not free.
>
> **THE QUESTION: should the harness default follow the browser, with the fingerprints re-minted once
> deliberately — or stay pinned, with the instruments that make look-like claims switched over
> individually?**

---

## NOTES FOR THE MORNING

- **THE GATE WAS GREEN ON ALL EIGHT MERGES, and no bcrypt timeout was ever forgiven.** Every merge ran
  `npm run verify` and every one exited 0. Six pieces were docs-only, so R15 routing correctly skipped
  the server suite; **PIECE 5 and PIECE 8 did run it and it passed** (33.0 s). **That is luck, not a
  fix** — PIECE 1 measured the suite sitting **21 ms** from its limit, and the flag is still absent.
- **The gate caught one real thing**, on PIECE 1: `check-index` failed because this very sheet was on
  master unregistered. Fixed rather than waved past.
- **PIECE 5 was the only build.** One file, two fields. Everything else diagnosed, designed or
  collected.
- **Two of my own errors are in the record rather than deleted.** A timing run at `75685b5b` bypassed
  the flag it was testing and would have named the wrong commit (GATE-RED-1 §a); and I expected
  `camera.state` to be a third render-fingerprint blindness when the renderer no longer reads it
  (RENDER-FINGERPRINT-BLIND-1 §7). Both are corrected in place in their reports.
- **One standing note is now wrong and was corrected:** garden-path does NOT silently yield zero any
  more — it completes 20/20, because your beetle decision made the race short enough. **The silence
  was never fixed; the symptom moved out from under it.**
