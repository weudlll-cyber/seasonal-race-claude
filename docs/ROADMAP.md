# RaceArena — Development Roadmap

> **THIS FILE IS A REDIRECT. It owns nothing.**
> Everything it held is in **[BACKLOG.md](BACKLOG.md)**:
>
> - **open work** → PART ONE
> - **completed phases, the session log, the status updates, and the phase-status table**
>   → PART TWO, *Phase history — moved whole from ROADMAP*
>
> **A new phase gets a row in that table and its work goes in the backlog.** Nothing gets added here.

## Why this file still exists

**It is a redirect and not a deletion, deliberately.** Eleven documents and reports link to
`docs/ROADMAP.md`, and several are reports — append-only by rule, so their links cannot be rewritten.
Deleting the path would break every one of them and turn a finished merge into a trail of dead links.
A stub costs nine lines and keeps them all landing somewhere true.

## What happened, in two steps

**ROADMAP-FOLD-1, 2026-08-23** (NIGHT-2026-08-23 piece 3), under the owner's decision **D24** —
*BACKLOG owns both, with ROADMAP reduced to a phase-status table*. It moved all 35 sections into
BACKLOG whole and unedited and left this file at 74 lines.

**ROADMAP-FOLD-2, 2026-08-27** folded the remaining table and replaced the file with this redirect.
The 74 lines were the reason two documents still half-owned one subject — which is the exact thing
D24 existed to end, so the merge was not finished while they stood.

**Both moves were MOVES, not audits.** No verdict was re-checked and no completion claim was
confirmed or withdrawn in either. **If a record in BACKLOG PART TWO and one in PART ONE disagree,
PART ONE is live.**
