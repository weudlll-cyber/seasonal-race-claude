# MORNING SHEET — 2026-09-23 · THE CHASE

Branch `feat/chase-after-outcome`, off master `32afd356`. **Nothing merged, nothing minted.**

## DONE

- **PIECE 1 — the mechanism is built, behind three keys whose defaults are today's race.**
  `chaseAfterOutcomeEnabled: false`, `chaseAfterOutcomeSelection: 'leader'`,
  `chaseAfterOutcomeSlots: 2`. Commits `0c9a290f` and `b4ec7ed4`, both pushed.
  - ★★ **All four fingerprints UNMOVED at the defaults** — the extension is not leaking into the
    shipped race. That was the night's stop condition and it is clear.
  - The leader brake is **never** extended: past the boundary the braked branch is skipped and the
    hero branch is zero, so the only non-zero director is the boost. A test pins it as "no command
    is lower than with the key off" — written that way because a racer braked *before* the boundary
    carries that value across it on both arms.
  - 10 tests over two files, including the wiring guard (all three keys reach the governor, both
    halves of the path) and a sabotage guard (boost zeroed ⇒ ON must equal OFF exactly).
  - Two things fixed while there: the `raceGovernor.js:169` JSDoc calling the rotation "default OFF"
    when it ships ON, and a `docs/FORCE-MAP.md` citation my own insertions had pushed out of range.

## RUNNING

- **PIECE 2 — stage 1, nine arms at N=30** (A0, L2–L5, G2–G5), quiet, ten tracks × seeds 1–3.

## OPEN

- PIECE 3 — stage 2, the survivors at N=300.
- PIECE 4 — the winner at wild (falls first if time runs short).
- PIECE 5 — report `CHASE-BUILD-1.md`, and the server on the recommended setting.

## NEEDS HIS WORD

- Nothing yet. The recommendation lands in PIECE 5; **nothing is minted or merged before his eye.**
