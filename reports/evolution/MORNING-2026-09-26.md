# MORNING 2026-09-26 — the night sheet

★ **PIECE 6's report is the first thing to read**:
[DEVSCREEN-GROUPINGS-1.md](DEVSCREEN-GROUPINGS-1.md) offers three ways to lay out the 205 dev-screen
controls — cheapest first — for the owner to choose from. No candidate renames a stored key; no
recommendation is stated.

**Off master `f6bc6c38` at launch. Merged as `c9408a48` — night branch deleted at origin, one head
remaining.**

**State at launch:**
- master head `f6bc6c38` (`merge(feat/stay-on-the-finish): STAY-ON-THE-FINISH-1 — the auto-advance switch gets a real job`)
- This machine: **14 logical cores, 12 physical.** Worker ceiling was 10 per the brief.
- Four fingerprints as they stand at launch — and still stand at close-out:
  - world       `81798e1875975cc2`
  - camera      `be48503a324429cb`
  - render      `90344c0f0361cbf1`
  - world-off   `172474674a9697b0`
- Working tree carried one pre-existing modification to `.claude/settings.json` — the brief forbids
  touching it, and the night did not.

---

## FINISHED

- **PIECE 0** ✅ — branch cut, sheet drafted and pushed, first heartbeat (`8e4cf131`).

- **PIECE 1** ✅ — all nine MISLEADING tooltips repaired (`2f5ff13c`). Race Defaults + Racer Editor
  rewritten to describe the actual effect; Auto-Scale "Disabled by default" removed; the two
  Dynamics "Default: 67%" trailers removed; the Camera Advanced weight-group blurb rewritten to
  describe the per-offer coin flip (including the endgame's LEAD_CHANGE exception). No behaviour
  changed. Inventory tallies regenerated to a third column:
  **204 MATCHES / 0 MISLEADING / 1 SUSPECTED DEAD / 1 RESERVED = 205.**

- **PIECE 2** ✅ — `scripts/check-tooltip-values.mjs` built, wired into
  `.github/workflows/ci.yml` and force-run under `verify --premerge` (`fe614551`). Sibling to
  `check-config-claims`: no config value in a UI string, one home in `defaults.js`, dated rows
  exempt. Ran on the whole screen — **46 findings beyond the nine PIECE 1 repaired**; 45 fixed in
  this piece as a removal-of-a-number (4 Auto-Scale, 2 more Dynamics, 32 Camera Advanced, 1
  NameTag, 1 RacerEditor), 1 sits on a dated comment line as history. Sabotage arms both
  directions: put a number back → guard red; remove → guard green. Guard test suite 5/5 green.

- **PIECE 3** ✅ (with a finding under NEEDS HIS WORD) — `comeback-precedence` re-pinned from the
  dead Garden Path 41000 to space-sprint seed 2, chosen from `scripts/diag/comeback-beats.mjs`
  (`ba6d8e8e`). Ran three times on three valid fixtures (seeds 2, 3, 5, all confirmed by the diag
  as casting a comebacker on the driver camera) — **all three failed identically: `the race never
  cut to a comeback at all`.** No `COMEBACK_ZOOM` state in any of the browser traces. No assertion
  touched; the spec is left RED, per the brief. Report:
  [COMEBACK-PRECEDENCE-REPIN-1.md](COMEBACK-PRECEDENCE-REPIN-1.md).

- **PIECE 4** ✅ — `scripts/lib/trackScope.mjs` grew a second entry point `resolveTrackScopeIds`
  (`2f175681`). Same refusal wording, same exit code, same provisions — one home, two doors. Five
  diag tools brought under the refusal: `company-ceiling-who`, `company-under-floor`,
  `endgame-spec`, `headcount-price`, `sprite-premise`. Per-tool sabotage arms verified
  (`--tracks=nope` refuses with exit 2, valid scope produces rows). Ten file-based analysers NAMED
  and left alone because their scope resists the refusal without changing shape. Guard test 7/7
  green. Report: [TRACKSCOPE-DIAG-REACH-1.md](TRACKSCOPE-DIAG-REACH-1.md).

- **PIECE 5** ✅ — two structural oddities established from source, not acted on (`1d928ff9`).
  `minTargetScreenPx` writes to two stores at two scopes and reaches no reader on the race path
  today; both values feed only their own preview / seed. The Race Tuning master reset covers the
  five RACE-RELEVANT blocks (including Auto-Scale in its own tab) and deliberately excludes the
  two COSMETIC blocks (Frame Timing inside Race Tuning, Camera in its own card). Written into
  `docs/DEVSCREEN-INVENTORY.md` § *Structural oddities established 2026-09-26*.

- **PIECE 6** ✅ — three candidate groupings for the 205 controls on the owner's desk
  (`68fa899f`). Candidate A re-tags with a fingerprint's-own-line badge, moves nothing (~1 day);
  Candidate B lifts Frame Timing to a new "What Watches It" group (~2-3 days, 2 controls moved);
  Candidate C splits Camera Advanced into six timeline cards (~1 week, 79 controls moved, some
  test-id churn). No candidate renames a stored key. No recommendation. Report:
  [DEVSCREEN-GROUPINGS-1.md](DEVSCREEN-GROUPINGS-1.md).

- **PIECE 7** ✅ — the close-out:
  - `docs/BACKLOG.md` PART ONE rows 1, 6 and 7 narrowed with dated NIGHT-2026-09-26 updates;
    `docs/OPEN.md` regenerated to `2026-09-26` (`b73fc4cd`). A subject appears in exactly one part.
  - Source hygiene: this night touched **twelve engineered files** — five DevScreen section
    sources (text only, no logic changed), one dev-screen inventory doc, one new guard
    (`scripts/check-tooltip-values.mjs`) + its test, one CI workflow step, five diag tools
    (import + boot-time refusal only), and `scripts/lib/trackScope.mjs` grew a second entry point
    + its test file. No dead code was left behind. File headers already carried module
    descriptions and were not disturbed. Six new files under `reports/evolution/` are the
    lab-journal record of the night and are indexed.
  - **`npm run verify -- --premerge` → PASS 21, FAIL 0, SKIP 15, 335.8 s wall clock.** The new
    tooltip-values guard is included and green. World fingerprint verified with
    `node scripts/fingerprint-default.mjs` after the run: `81798e1875975cc2` — unmoved from launch.
  - **Merged `night/2026-09-26` into master as `c9408a48`; pushed to origin; night branch deleted
    at origin.** `git ls-remote --heads origin` returns exactly one head: `master → c9408a48`.
  - CI status on master `c9408a48` **was not fetched**: `gh run list` is outside this session's
    permission set (same as the prior night). The evidence for the merge is the local
    `verify --premerge` PASS and the fingerprint unchanged.
  - **Dev server**: this session's Bash processes are reaped at close-out and PowerShell's
    `Start-Process` (the standard detached-process form on Windows) is outside this session's
    permission set. So the dev server was not left running for the morning; the owner will need
    to start it himself via the `dev-start` skill, exactly as recorded on 2026-09-25's sheet.
    **The behaviour the sheet flagged as a FACT that day — that his config badge read
    `0 race / 11 cosmetic` — was NOT re-checked tonight, and NOTHING in this session's changes
    touched his stored configuration.**

## RUNNING

Nothing. Seven pieces closed, the merge is in.

## OPEN — regenerated

- Nothing from this night is open. What stays open is the WORK it did not touch:
  `docs/OPEN.md`'s seven rows (2026-09-26 snapshot), each narrowed where this night touched it
  and otherwise unchanged.

## NEEDS HIS WORD

- **The `comeback-precedence` finding from PIECE 3.** Three space-sprint seeds (2, 3 and 5) that
  the harness diag confirmed CAST a comebacker (`written [#N@resolve 0.7]`) and confirmed the shot
  as SHOWN on the driver camera produced NO `COMEBACK_ZOOM` state at all in the browser prod arm
  — over three separate 2.3 min runs, one per fixture. The `comeback-precedence` spec is now RED
  on a fixture that IS valid (the plan casts one) rather than the previous fixture that was quiet
  (Garden Path 41000, which cast nobody). The precedence's forced-transition logic is untested by
  this evidence because the shot never appears at all. Owner questions the sheet cannot answer:
  does the browser's director decline the comeback offer preferentially against BATTLE /
  LEAD_CHANGE, or does the shot's own gate (start-gap, current-rank-pct, positions-gained) reject
  the candidate in the browser's rank history — and is the `comebackWeight` value the shipped
  picture wants at its shipped setting? Full evidence:
  [COMEBACK-PRECEDENCE-REPIN-1.md](COMEBACK-PRECEDENCE-REPIN-1.md).

- **The PIECE 6 groupings.** Three candidates for `B-UX2` are on his desk — A, B, or C. This is
  the question the row has been waiting for. A one-sentence choice unblocks the reorganisation.
