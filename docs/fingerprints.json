{
  "_": [
    "THE ONE HOME for the three current fingerprints. No document carries a copy — not even a",
    "generated one. Other documents REFERENCE this file, or say nothing.",
    "",
    "ONE-TRUTH-1 made 19 copies machine-written and machine-checked. That was the wrong shape: a",
    "generated copy is still a copy, it still has to be kept in step, and a reader who finds one has",
    "no way to know whether it is the truth or a stale rendering of it. ONE-TRUTH-2 (the owner's",
    "order) deleted all 19. Where a document's argument needed the reader to know a fingerprint",
    "EXISTS, it now points here; where it did not, the mention was removed.",
    "",
    "WHERE A CURRENT VALUE MAY STILL APPEAR, and nowhere else:",
    "  - this file",
    "  - docs/TAGS.md — the tag register. A tag's hash is what it was when the tag was cut.",
    "  - reports/ — the lab journal. Append-only by rule: a report records what was true on the day",
    "    it was written and must never be rewritten. Listed as a PREFIX rather than 112 filenames,",
    "    which is a decision: enumerating them would be noise that nobody would maintain, and the",
    "    append-only rule already makes every one of them history by construction.",
    "",
    "TO RE-MINT: run the role's `reproduce` command, put the new value here, and commit. There is",
    "nothing else to update — that is the point.",
    "",
    "WHICH DOCUMENT OWNS WHAT, resolved because four of them implied they owned this:",
    "  - THIS FILE owns the CURRENT VALUES. Nothing else does.",
    "  - docs/SIM.md owns the LINEAGE — which change moved which value to which. Its head points here.",
    "  - reports/parity/REBASELINE.md owns WHAT MOVED each one, and now carries no values.",
    "  - docs/SHIP-CEREMONY.md owns the PROCEDURE for minting."
  ],
  "roles": {
    "world": {
      "value": "dc4647be0f55ebdb",
      "covers": "the RACE: physics, plan, outcome",
      "reproduce": "node scripts/fingerprint-default.mjs",
      "mintedOn": "3518ee0b",
      "mintedBy": "RACER-MOTION-2 — ship lateral acceleration cap (maxLateralAccelPerStep 0.0005)",
      "date": "2026-07-31",
      "lastVerified": "2026-08-18 — RE-RUN IN FULL on the ENDGAME-THRESHOLD-095 merge 740f605c and UNMOVED. Run rather than argued from a closure statement: defaults.js IS inside this instrument's declared closure (36 files) because the race's keys and the camera's share one file, so only the CONTENTS of the diff make this a camera-only change and only a measurement can say so. Previously 2026-08-17 — NOT re-run at SHIP-RUNIN-1, and that is the stronger answer rather than a skipped step: walking scripts/lib/routing.mjs closureOf from this instrument's declared reach entries gives 36 files, and NONE of the 17 files the merge puts on master is one of them. The ship's only production file is CameraDirector.js, which is outside this closure and inside CAMERA's and RENDER's. engine-reach was deliberately not leaned on: on a committed merge it compares the working tree and can say nothing about the merge. Previously 2026-08-12 — re-run in full on eea0acf2 (the RUNIN-1 merge) and UNMOVED. Run rather than argued from engine-reach: defaults.js IS in the engine hull and this merge genuinely edits it, and engine-reach --check compares the WORKING TREE, so on a tree whose merge is already committed it reports byte-identical and can say nothing about the merge. The two keys it adds are camera keys, which is why the race is untouched — but that is the conclusion the measurement supports, not the reason to skip it. Previously 2026-08-12 — re-run at CLEAN-STATE-1."
    },
    "camera": {
      "value": "d9f45a4aea0e5778",
      "covers": "the DIRECTOR's decisions: state, phase, anchor, zoom, offsets, camT, targets — INCLUDING the ending the director composes after the last crossing (CAMERA-ENDING-WINDOW-1)",
      "reproduce": "node scripts/camera-fingerprint.mjs --quiet",
      "mintedOn": "740f605c",
      "mintedBy": "ENDGAME-THRESHOLD-095 — THE ENDGAME OPENS AT 95% OF THE WAY TO THE FINISH RATHER THAN 90%. THE OWNER'S DECISION, 2026-08-18: he had been running 0.95 himself, judged it on a production build on 2026-08-17, and EXPLICITLY WAIVED a before/after sweep — so no ten-track measurement stands behind this number and none is claimed. ONE KEY IN defaults.js and nothing else; the Dev control, its range and its 1% step are untouched. WHY THE CAMERA MOVES SO MUCH FOR ONE VALUE: that key is read in two places and both are the run-in's — the state machine's endgame gate (leaderProgress > endgameThreshold -> LEADER_ZOOM) and _runInWindowOpen, where the finish-line bound starts composing. So the endgame declares later AND the run-in's window is half as long. THIS IS THE FIRST CAMERA SHIP IN THIS SEQUENCE WHERE STATE DECISIONS MOVED rather than only framing, and the tracking-lag frame counts say so: BATTLE_ZOOM 9406 -> 9701, COMEBACK_ZOOM 605 -> 644, LEADER_ZOOM 17788 -> 17630, LEAD_CHANGE 7789 -> 7786, with OVERVIEW and PHOTO_FINISH identical because neither is eligible in the stretch the threshold moved. Every tail improves, because the opening is the steepest zoom in the race and a later window spends less of each state inside it. WORLD AND WORLD-OFF WERE MEASURED, NOT ARGUED, and the closure walk is why: defaults.js is inside ALL FOUR instruments' declared closures — the race's keys and the camera's live in one file — so 'it is only a camera key' is a claim about the CONTENTS of the diff and not about the file it sits in. Both are byte-identical (dc4647be0f55ebdb / 854018ee5d3d83e1). engine-reach was deliberately not leaned on: on a committed merge it compares the working tree and its verdict is not evidence. A STORED 0.95 IS DROPPED by this change — confirmed against the real pruneStored — so the owner follows the default at the same number and the key stops being shadowed. Replaces 6ae77f12daf23f78 (SHIP-RUNIN-1).",
      "date": "2026-08-18",
      "lastVerified": "2026-08-18 — measured fresh on the merge commit 740f605c (no --cheap), alongside WORLD and WORLD-OFF, which were RUN rather than argued because defaults.js is inside their closure too.",
      "supersededMintedBy": "SHIP-RUNIN-1 — THE RUN-IN HOLDS THE OPENING SHOT AND CLOSES ONCE, AND THE FINISH LINE STAYS IN FRAME. THE OWNER JUDGED THIS ON A PRODUCTION BUILD ON 2026-08-17 AND ACCEPTED IT; this hash is that acceptance written down. THREE CHANGES MOVE IT AND ALL THREE ARE FRAMING, NOT STATE: (1) RUNIN-LINE-1 — the corridor cap no longer overrides the line's own ceiling. `_setTargets` honoured `_ceilings.line` in its `Math.min` and then let the corridor cap raise the composed zoom back above it, so the one term that knows where the finish line is was being overruled by a term that does not. The repair is one clause, and an instrument was built BEFORE it rather than after: `check-runin-frame` gained a THIRD question — is the line in frame from the endgame threshold to the crossing, on ten tracks — which splits its losses by cause (overridden vs merely trailed) so a future regression names itself. (2) RUNIN-HOLD-1 — the opening shot is HELD and then closed in ONE sweep. Left alone `_lineCeiling` begins closing on the frame the window opens, which measured about 3.6 s of lead-in at roughly 95 px/s of picture flow, below the rate at which anything reads as movement. The release is DERIVED, not chosen: the sweep lasts `runInOpenMs`, so it begins when the leader's remaining time to the line — measured over the RUN-IN's own span, because the field decelerates into the finish and a whole-race average was wrong by about six times — has fallen to that length. No key was added. (3) RUNIN-BACK-1 — the leader's placement is the owner's again, a little BEFORE the centre of frame easing to a little AFTER it, and RUNIN-AHEAD-1's forward bound, which contradicted that specification, is removed with nothing in its place: placing the leader behind centre is ITSELF the reason the frame does not reach past the line. It narrowed nothing — WHY-SO-WIDE-1 measured that the extra width the bound bought carried no racer, the whole field spanning 600-830 px inside a frame the line was forcing to 2668. DEFAULTS ARE NOT TOUCHED: `endgameThreshold` keeps its shipped value and the Dev control merely steps it in 1% instead of 5%. WHY WORLD AND WORLD-OFF CANNOT MOVE, decided by the instruments' own declarations rather than asserted: walking `scripts/lib/routing.mjs` `closureOf` from each declared reach entry, NONE of the 17 merged files lies inside `fingerprint-default.mjs`'s closure (36 files), while both this instrument's closure (36) and RENDER's (55) contain `CameraDirector.js` — the single production file this ship changes. `engine-reach` was deliberately NOT leaned on: on a committed merge it has no working-tree diff to read and its verdict would not be evidence. FIVE FURTHER SHAPES WERE BUILT, MEASURED AND REVERTED on the branch and none is in this tree — RUNIN-PIN-1, RUNIN-ANCHOR-1, RUNIN-RATE-1, RUNIN-EVEN-1/-2 and RUNIN-SCHEDULE-1. Replaces ff2bc42af377b5cf (CONTENDER-ZOOM-1).",
      "supersededDate": "2026-08-17",
      "supersededOn": "48f954a4",
      "supersededNote": "6ae77f12daf23f78 was SHIP-RUNIN-1's value, minted on 48f954a4. It is the last state of this hash before the endgame threshold moved."
    },
    "render": {
      "value": "1274c7e8444238e3",
      "covers": "the DRAW CALL SEQUENCE: sprite placement, text, styles, transforms, layer order",
      "reproduce": "node scripts/render-fingerprint.mjs --quiet",
      "mintedOn": "740f605c",
      "mintedBy": "ENDGAME-THRESHOLD-095 — the same ship as CAMERA above, and this hash moves for the reason that one does: a later endgame changes which state the director is in and how the run-in frames it, so the transform on the sampled endgame frames changes. NOTHING IN THE DRAWING ITSELF CHANGED — the diff contains no drawing file at all. WORLD and WORLD-OFF were measured and are unmoved. Replaces a870f5f9e79cb444 (SHIP-RUNIN-1).",
      "date": "2026-08-18",
      "lastVerified": "2026-08-18 — measured fresh on the merge commit 740f605c (no --cheap), alongside WORLD and WORLD-OFF, which were RUN rather than argued because defaults.js is inside their closure too.",
      "supersededMintedBy": "SHIP-RUNIN-1 — the same ship as CAMERA above, and this hash moves for the reason that one does: the run-in composes a different shot through the whole endgame, so the transform on every drawn frame of that window changes. NOTHING IN THE DRAWING ITSELF CHANGED — no sprite, no text, no layer order, and no file under `renderRaceFrame.js` is in the diff. The move is entirely the camera's, and it is visible here only because RENDER's declared closure (55 files) contains `CameraDirector.js`. WORLD and WORLD-OFF were NOT re-run: no merged file lies inside `fingerprint-default.mjs`'s computed closure, which is a stronger statement than a matching hash and is the one the closure can make. Replaces 0e04fa4a5e9c3b85 (MINIMAP-MARKS-1 + MINIMAP-TAIL-1).",
      "supersededDate": "2026-08-17",
      "supersededOn": "48f954a4",
      "supersededNote": "a870f5f9e79cb444 was SHIP-RUNIN-1's value, minted on 48f954a4. It is the last state of this hash before the endgame threshold moved."
    },
    "world-off": {
      "value": "854018ee5d3d83e1",
      "covers": "the world with gap-reroll OFF — the pre-feature invariant that proves the shipped change is the only thing that moved",
      "reproduce": "node scripts/fingerprint-default.mjs off --gapRerollEnabled=false",
      "mintedOn": "3518ee0b",
      "mintedBy": "RACER-MOTION-2",
      "date": "2026-07-31",
      "lastVerified": "2026-08-18 — RE-RUN IN FULL on the ENDGAME-THRESHOLD-095 merge 740f605c and UNMOVED. Run rather than argued from a closure statement: defaults.js IS inside this instrument's declared closure (36 files) because the race's keys and the camera's share one file, so only the CONTENTS of the diff make this a camera-only change and only a measurement can say so. Previously 2026-08-17 — NOT re-run at SHIP-RUNIN-1, for the same closure reason as the ON world above: this is the same instrument under a flag, so it has the same 36-file closure and the same answer. Previously 2026-08-09 — re-run at SHIP-ROUTING and UNCHANGED, so the pre-feature invariant holds either side of this ship too. SHIP-THREE did not re-run it: nothing in that ship touches the engine (engine-reach clears every changed path except defaults.js, whose only moved key is read by the camera), and the ON world is unchanged, so the OFF arm has no question to answer. Previously re-run at the SHIP-THE-LINE merge and UNCHANGED, so the pre-feature invariant still holds either side of that ship. Previously 2026-08-06 — RESOLVED and VERIFIED by running it. This was UNKNOWN in ONE-TRUTH-1. docs/SIM.md's lineage table names the flag; the first command written here OMITTED THE LABEL argument, argv[2] swallowed the flag as a label, and the run returned the ON value while printing 'default config'. The label is mandatory. With it, this value reproduces exactly. Re-run at RENDER-SAMPLER-CEREMONY on 0308f7b9 and UNCHANGED, measured rather than argued because the ON world was measured too."
    }
  },
  "historyHomes": [
    "docs/TAGS.md",
    "reports/"
  ],
  "machineExceptions": [],
  "_machineExceptionsNote": [
    "Files that must contain a current fingerprint as a LITERAL because something reads it — a test,",
    "a baseline, a script. Listed BY NAME, never by pattern, each with its reason.",
    "",
    "The list is EMPTY, and that was measured rather than assumed: every tracked file was scanned and",
    "not one test, script or config consumes a fingerprint string. All 19 former copies were prose or",
    "comments meant for a human. If you are about to add one, make it read this file instead."
  ]
}
