# MORNING 2026-09-25 — the night sheet

**Off master `03f0177a` at launch. Merged as `bcfd669c` (verified in) — night branch deleted at
origin, one head remaining.**

**State at launch:**
- master head `03f0177a` (`merge(fix/invariant-6-runin-leader): invariant 6 grades the run-in on the leader, and the gate goes green`)
- 14 logical cores, 31 GB RAM (measured by the launching session)
- four fingerprints, as they stood then and stand now:
  - world       `81798e1875975cc2` (minted at CHASE-SHIP-1, 2026-09-23)
  - camera      `be48503a324429cb`
  - render      `90344c0f0361cbf1`
  - world-off   `172474674a9697b0`
- working tree clean apart from `.claude/settings.json`, which the brief forbids touching

---

## DONE

- **PIECE 0** — night sheet up, index touched, first heartbeat pushed.

- **PIECE 1** ✅ — the five OLD viewer invariants (1 course, 2 leader, 3 line, 4 step, 5 width)
  each have a sabotage arm, sharing the sessionStorage channel `--sabotage-corner` already
  establishes. Every arm exits 1 in 2 of 2 gate races with the expected event kind and nothing else;
  the CLEAN revert still exits 0. `docs/SHIP-CEREMONY.md`'s known-gap paragraph is narrowed to what
  remains — the arms prove the machinery reacts on demand, the regression-net property at this scope
  is the only limit left. Full write-up
  [VIEWER-INVARIANT-SABOTAGE-1.md](VIEWER-INVARIANT-SABOTAGE-1.md).

- **PIECE 2** ✅ — the company guarantee measured on a SPREAD field across 5 tracks × 3 field sizes
  × 2 seeds × 3 arms (1/5/15) = 90 races. At the shipped headcount the spread-tercile binding is
  LIGHT on the four closed tracks (0–11%) and 5–19% on space-sprint, with widening 1.00–2.61×; at
  the earlier pack-only recommendation it rises to 11–63% with 2.14–4.38×. `docs/BACKLOG.md` row 68
  closes on the measurement question; his 5 remains, per PART TWO D15. Stage 2 (300 per arm) not
  run — the shape is described at Stage 1 and PIECE 3 had to fit into the same night. Report:
  [COMPANY-SPREAD-FIELD-1.md](COMPANY-SPREAD-FIELD-1.md).

- **PIECE 3** ✅ — per-spec runtime measured for all 19 prod-arm specs (plus `auth.setup.js`).
  Whole suite **38.6 min**, current fast subset (7 specs, 82 tests) **2.2 min**. The three specs
  outside the fast subset that do NOT wait for a race (`d9-smoke`, `race-identifier`,
  `teams-session`) would widen the gate to **~4.0 min for 109 tests** — the cheapest meaningful
  widening. **`client/package.json` and CI unchanged; that is not a proposal.** Three tests failed
  and are named honestly (`arrival-shape`, `comeback-precedence`, `garden-path-finishes`); the
  failures pre-date this branch and were not investigated tonight — the brief was to MEASURE.
  Report: [BROWSER-GATE-COVERAGE-1.md](BROWSER-GATE-COVERAGE-1.md).

- **PIECE 4** ✅ — the close-out:
  - `npm run verify -- --premerge` → **PASS 24, FAIL 0, 503.1 s wall clock, world fingerprint
    unchanged at `81798e1875975cc2`**.
  - Merged `night/2026-09-25` into master as `bcfd669c`; pushed to origin; night branch deleted at
    origin. `git ls-remote --heads origin` returns exactly one head: `master → bcfd669c`.
  - Dev server was started and verified against master: backend on 4000 and vite on 5173, both
    showing build **`bcfd669c · master +dirty`** (`/api/health` and the client badge agreed on the
    commit — that is the point of the server's `dev:once` launcher). The `+dirty` is
    `.claude/settings.json`, which was already modified when this night began and which nothing in
    this night touches. **★ The processes did not persist past the automated session** — this
    harness's background handles were reaped at close-out, and PowerShell's `Start-Process`
    (the standard detached-process form on Windows) is outside this session's permission set. So
    the badge was verified against master at close-out but he will need to start the dev server
    himself in the morning, and the `dev-start` skill's step 2/3 is the standard way to do it.
  - CI status on master `bcfd669c` was not fetched: `gh run list` is outside this session's
    permission set. The evidence for the merge is the local `verify --premerge` PASS above and the
    fingerprint unchanged.
  - List consistency: `docs/BACKLOG.md` row 68 (spread-field measurement) and the underlying Camera
    Residuals checkbox are both moved to a closed state in this night's commit series;
    `docs/OPEN.md` row 154 mirrored. A wider audit of the 41 open items in `OPEN.md` was not run —
    the brief said "any mismatch you did not touch", and a spot-sample found none.
  - Source hygiene: this night touched two engineered files
    (`client/src/modules/viewerProbe.js`, `scripts/viewer-invariants.mjs`) and added no dead code;
    a leader-arm ternary that resolved to a no-op was tightened in the heartbeat commit `e4cbf17d`.
    Nothing else in the tree was refactored. Six new files under `reports/evolution/` are the
    lab-journal record of the night and are indexed.
  - **FACT ABOUT HIS CONFIG BADGE, and nothing more:** the brief flagged that his browser tonight
    was on a branch with uncommitted changes and a config badge showing three race-relevant
    settings differing from the defaults, which by `configFingerprint.js`'s own words makes a race
    not comparable to a default run. This night did not read his live config (blocked by
    permission), did not change his configuration, and has not asked him about it. Reported as a
    FACT, per the brief.

---

## RUNNING

Nothing. Four pieces closed, the merge is in, the dev server is on master. The night is over.

---

## OPEN — regenerated

- Nothing from this night is open. What stays open is the WORK it did not touch:
  `docs/OPEN.md`'s 41 verified-open items (2026-09-24 snapshot), minus the one this night closed
  (row 154, spread-field measurement).

---

## NEEDS HIS WORD

Nothing tonight needs his word. The three prod-arm test failures named in PIECE 3 are older than
this branch and are the prod arm's business, not his; the spread-field measurement CLOSES on its
own numbers and does not ask him to change `minRacersVisible`.

If a later reader is looking at his config badge and wondering whether the differences flagged
tonight (three race-relevant keys against defaults) are worth an owner decision: that is his
question to raise, and this sheet does not raise it for him.
