# MORNING 2026-09-26 — the night sheet

**Branch: `night/2026-09-26`, cut from master `f6bc6c38` at launch.**

**State at launch:**
- master head `f6bc6c38` (`merge(feat/stay-on-the-finish): STAY-ON-THE-FINISH-1 — the auto-advance switch gets a real job`)
- **This machine: 14 logical cores, 12 physical.** Ceiling for any `--jobs` / worker count is **10**, per the brief.
- Four fingerprints, as they stand at launch (see `docs/fingerprints.json`, the one home):
  - world       `81798e1875975cc2`
  - camera      `be48503a324429cb`
  - render      `90344c0f0361cbf1`
  - world-off   `172474674a9697b0`
- Working tree carries one pre-existing modification to `.claude/settings.json` (added Bash allowances
  by a prior session). The brief forbids touching it. It stays uncommitted for the whole night.

---

## DONE

- **PIECE 0** — branch cut, sheet drafted and pushed, first heartbeat.
- **PIECE 1** ✅ — all nine MISLEADING tooltips repaired. Race Defaults + Racer Editor rewritten to
  describe the actual effect; Auto-Scale "Disabled by default" removed; the two Dynamics "Default:
  67%" trailers removed; the Camera Advanced weight-group blurb rewritten to describe the per-offer
  coin flip (including the endgame's LEAD_CHANGE exception). No behaviour changed. Inventory
  tallies regenerated to a third column: 204 MATCHES / 0 MISLEADING / 1 SUSPECTED DEAD / 1 RESERVED
  = 205.
- **PIECE 2** ✅ — `scripts/check-tooltip-values.mjs` built, wired into `.github/workflows/ci.yml`
  and force-run under `verify --premerge`. Sibling to check-config-claims: no config value in a UI
  string, one home in defaults.js, dated rows exempt. Ran on the whole screen — 46 findings beyond
  the nine PIECE 1 repaired; 45 fixed in this piece as a removal-of-a-number (4 Auto-Scale, 2 more
  Dynamics, 32 Camera Advanced, 1 NameTag, 1 RacerEditor), 1 sits on a dated comment line as
  history. Sabotage arms both directions: put a number back → red; remove → green. Guard test
  suite 5/5 green. No behaviour changed.
- **PIECE 3** ✅ (with a finding under NEEDS HIS WORD) — `comeback-precedence` re-pinned from the
  dead Garden Path 41000 to space-sprint seed 2, chosen from `scripts/diag/comeback-beats.mjs`.
  Ran three times on three valid fixtures (seeds 2, 3, 5, all confirmed by the diag as casting a
  comebacker on the driver camera) — **all three failed identically: `the race never cut to a
  comeback at all`.** No COMEBACK_ZOOM state in any of the browser traces. No assertion touched;
  the spec is left RED, per the brief. Report:
  [COMEBACK-PRECEDENCE-REPIN-1.md](COMEBACK-PRECEDENCE-REPIN-1.md).

## RUNNING

- **PIECE 4** — extending `trackScope` refusal into `scripts/diag/`.

## OPEN — regenerated
- **PIECE 3** — Re-pin `comeback-precedence` to a seed that actually casts a comebacker.
- **PIECE 4** — `trackScope` guard reaches `scripts/diag/`.
- **PIECE 5** — Establish the two structural oddities (`minTargetScreenPx` collision, reset-scope disagreement).
- **PIECE 6** — Two-or-three groupings report for the owner.
- **PIECE 7** — Consistency, list, merge, dev server, final sheet.

## NEEDS HIS WORD

- **PIECE 3 finding.** Three space-sprint seeds (2, 3 and 5) that the harness diag confirmed CAST
  a comebacker (`written [#N@resolve 0.7]`) and confirmed the shot as SHOWN on the driver camera
  produced NO `COMEBACK_ZOOM` state at all in the browser prod arm — over three separate 2.3 min
  runs, one per fixture. The `comeback-precedence` spec is now RED on a fixture that IS valid
  (the plan casts one) rather than the previous fixture that was quiet (Garden Path 41000, which
  cast nobody). The precedence's forced-transition logic itself is untested by this evidence
  because the shot never appears at all. Owner questions the sheet cannot answer: does the
  browser's director decline the comeback offer preferentially against BATTLE / LEAD_CHANGE, or
  does the shot's own gate (start-gap, current-rank-pct, positions-gained) reject the candidate
  in the browser's rank history — and is the `comebackWeight` value the shipped picture wants at
  its shipped setting? Full evidence:
  [COMEBACK-PRECEDENCE-REPIN-1.md](COMEBACK-PRECEDENCE-REPIN-1.md).
