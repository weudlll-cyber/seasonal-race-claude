# NIGHT 2026-09-24D — the resumed night, six pieces finished

**Branch `feat/night-2026-09-24c`, continuation of NIGHT-2026-09-24C. Pieces 0, 1, 2 and 3(a) had
already been done at the start of this run; pieces 3(b), 3(c), 4, 5 and 6 landed tonight; piece 7
was deferred as the brief permits. Piece 9 is this file and the merge.**

★★ **WHAT IS WAITING ON HIM, FIRST OF ALL** — the ruling of 2026-09-24 on a PAIR at the front is
STILL PENDING and this run has built nothing that depends on it. He said a pair is acceptable IF
IT IS NOT TOO OFTEN, and has not yet judged the measured frequency. The four SOLO cases (the
picture he already objects to) and the nine PAIR cases (undefined, never ruled on) are recorded in
the morning sheet under NEEDS HIS WORD, in [BACKLOG.md](../../docs/BACKLOG.md) PART ONE, and
here — with rates:

- SOLO: quiet **3/300 = 1.0%**, wild **1/300 = 0.3%** —
  `dirt-oval` seed 20 @ 205 px (worst), `ice-track` 10 @ 163, `ice-track` 23 @ 159,
  wild `dirt-oval` 3 @ 166.
- PAIR: quiet **7/300 = 2.3%**, wild **2/300 = 0.7%** —
  `ice-track` 20 @ 214, `space-sprint` 27 @ 189, `dirt-oval` 24 @ 184, `searound` 19 @ 175,
  `searound` 24 @ 172, `space-sprint` 21 @ 167, `luger-hill` 20 @ 166,
  wild `dirt-oval` 9 @ 208, wild `garden-path` 13 @ 202.

---

## Per-piece verdicts

### PIECE 3(b) — one shared `RangeSlider` component ✅

DONE, merge `77c6d6f7`. The wrapper `<div>` and the `<input type="range">` were duplicated at three
sites — twice in `BrandingProfiles` (Logo Size, Logo Opacity) and once inside
`SurfaceClassManager`'s `ConfigFields`. Extracted to
`client/src/screens/DevScreen/components/RangeSlider.jsx`; the display `<span>` stays at the call
site as `children`, so no visible property changes.

**★ The brief said "three sections". By count it is three INSTANCES across TWO sections.** The most
conservative reading was to remove the actually-shared markup rather than to invent a third site.
Recorded in the commit and here.

Full client suite: 268 files, 4761 tests, green.

### PIECE 3(c) — tooltips for the six sections that had none ✅

DONE, merge `ff4ee561`. `InfoTooltip`s added to `SystemSettings`, `RaceTuning`, `ConfigExport`,
`SurfaceClassManager`, `UserManagement`, `ChangePassword`. Reused the existing `InfoTooltip`
component the other sections already use.

**★★ A DEFECT the tests found:** three exact-string `getByLabelText` matches in
`ChangePasswordSection.test.jsx` broke when the label absorbed the tooltip's icon `<span>`.
Changed to anchored regex (`/^Current password/`, `/^New password/`, `/^Repeat new password/`) —
prefix-anchored, still uniquely identifying, robust to the addition.

Every string quoted in a tooltip was read from source — the SurfaceClass Kind vocabulary from
`KindBadge`, the role default from `useState('operator')`, the 40-char label max from the
`maxLength={40}` attribute, and so on. `check-config-claims` covers NUMBERS only; the STRINGS were
covered by reading.

### PIECE 4 — the migration ledger ✅

DONE, merge `6a379b1d`. New: `scripts/migrate.mjs` and its 8-test `scripts/migrate.test.mjs`.

The runner reads `<dataRoot>/migrations.json`, applies every id not already there, and refuses to
re-run any recorded id. The RULE for pending-vs-applied is written in full in the file header:
ledger entry means applied; otherwise `alreadyAppliedByObservableState({ dataRoot })` reads the
DATA to decide — for `teams-1`, that means "every user carries a well-formed team". If yes, the
ledger is backfilled with `backfilled: true` and the migration is NOT run.

**★ Sabotage-proven:** a test whose name says so replaces the ledger write with a no-op and
asserts the double-run test then FAILS with runs=2 instead of 1. If it did not, the double-run
test would be decoration.

`docs/DEPLOYMENT.md` step 6 now names one command instead of "read the migrations section and
decide".

**Verified against the real `server/data/`:** `node scripts/migrate.mjs --status` prints
`teams-1: backfilled-from-state` — the observable-state fallback correctly detects an already-
migrated instance and does not re-run.

### PIECE 5 — the racer config folder move ✅

DONE, merge `d6df5184`. 80 tracked files moved from `client/src/modules/racer-types/` to
`client/src/racer-types/`. 29 external importers and 13 scripts had their paths shifted; internal
upward imports (`../storage/`, `../../services/`) re-pathed to the new depth. Path headers inside
the moved files updated to match.

**★★★ THE RACE DID NOT CHANGE.** All four fingerprints re-minted 4/4, 0 stray copies. The RACE
HULL stayed at 202 files. Both hull documents regenerated: `docs/SHIP-CEREMONY.md` (files under
`modules/`: 119 → 77) and `docs/SIM.md` (202 files re-listed at their new paths). The move keeps
every reaching edge, at a new address.

**★ A DEFECT that surfaced from prettier:** the pre-commit hook collapsed a multi-line import in
`client/src/screens/RaceScreen/index.jsx` — 4 lines shorter — which shifted line numbers on
subsequent citations. Six citations in `docs/FORCE-MAP.md` and one in `docs/branding.md` (all to
`RaceScreen/index.jsx#Lxxx`) went stale by −4. Each cite corrected to point at the current line.
Verified by re-running `check-fallback-agreement.mjs` — 0 disagree.

Two `MEASURED:` stamps went stale on the freshness dimension because the move touched files in
their closure without changing content: `docs/CAMERA_DIRECTOR.md` (tracking-lag) and
`docs/ENDING-PHASES.md` (straggler-truth). Re-stamped to `@ d6df5184` in the SAME commit as PIECE
6; the digits above the stamps are unchanged by construction.

### PIECE 6 — the frame-camera guard ✅

DONE, merge `ee50c9f8`. New: `scripts/check-frame-camera-inputs.mjs` + 5-test
`scripts/check-frame-camera-inputs.test.mjs`.

The guard scans `client/src/` for any `.js`/`.jsx` file that spells `anchorRacerIndex:`,
`comebackLockedRacerIndex:` or `runInArrived:` as an object-literal key outside `frameCameraInputs.js`
and its named exceptions (`CameraDirector.js` and two tests). The markers are read from
`FRAME_CAMERA_FIELDS` in the ONE HOME; `state` and `hudState` are DELIBERATELY excluded from the
marker set as too generic — the shape of the 2026-09-05 defect catches on the three unique-enough
names.

**★ Sabotage-proven:** a test named in full — *"SABOTAGE — a hand-written literal in a fixture
makes the guard RED"* — plants `{ state: 'FOO', anchorRacerIndex: 3 }` in a fixture and asserts
exit 1. If it did not, the guard would be decoration.

**LOUD-FAILURE RULE upheld:** zero files scanned OR an unreadable `frameCameraInputs.js` FAILS —
tested. A guard that passes because it found nothing to check is a no-op.

Ran on the live tree: 540 files scanned, 3 markers checked, no findings — as expected on the tree
the repair is already applied to.

### PIECE 7 — company guarantee on a spread field ⏸ DEFERRED

The brief marks this "FIRST TO FALL if time runs short". Deferred with a stated reason:

- The **ALGORITHM** is already unit-tested: `client/src/modules/camera/companyHeadcount.test.js`
  proves that for a strung-out field (12 racers at 100 px steps) and `minVisible=5`, the guarantee
  returns a zoom ceiling that places exactly 5 racers in shot. That test alone does not close the
  brief's question, which is about live races.
- What the brief asks for is a LIVE measurement: drive races on `quiet` across 10 tracks and
  count how often the guarantee BINDS (i.e. is the tightest constraint) and whether OTHER camera
  constraints outrun the guarantee's promise. That instrument needs a headless CameraDirector
  loop plus a per-frame headcount computation plus the sabotage test on top, and building both
  cleanly at 20:00 with PIECE 9 still ahead was not a good trade.
- The item stays in `BACKLOG.md` PART ONE under *"the company guarantee on a SPREAD field"* and
  in `OPEN.md` section 4, both unchanged.

---

## Counts

- **Open at start of run** (PART ONE, per OPEN.md scoreboard): 41.
- **Closed by this run**: 5 (PIECES 3b, 3c, 4, 5, 6). Each is in OPEN.md as strikethrough with what
  closed it, and in BACKLOG.md PART ONE with the same closure marker in the SAME COMMIT as the
  work (three commits do this — the last three).
- **Narrowed**: 0. The frame-camera-guard entry was ALREADY narrowed to "only the guard half is
  left" at the start; it moves to closed, not narrowed.
- **Superseded**: 0.
- **Newly discovered**: 0 architectural findings; 2 execution defects surfaced and fixed inline
  (the ChangePassword `getByLabelText` breakage in PIECE 3c, the prettier collapse in PIECE 5).
- **Open at end of run**: 36.

## The three buckets, PART ONE

- **NEEDS HIS WORD**: 8 items in OPEN §1 (12 rows, four already marked done/decided), plus the
  deployment items in §5 that are "his word + a purchase" — about 10-11 total.
- **NEEDS HIS EYE**: 5 items in OPEN §3, all live-in-browser questions.
- **NEEDS NEITHER**: 3 in §2 (after tonight's closures), 4 in §4 (still owed measurements
  including PIECE 7's company-guarantee-spread item), plus what remains delivery-side in §5. About
  8-10 total.

**★ THE THIRD BUCKET IS NEARLY EMPTY.** Section 2 was 8 open items at the start of tonight and
this run closed 5 of them; the section is down to 3. Whether "finished" is close depends on
whether the his-word/his-eye items count as finishable without him.

## ★★ THE MERGE WAS NOT PERFORMED — verify --premerge went red on viewer-invariants

The brief's merge rule requires "everything green and all four fingerprints unmoved". The
fingerprints ARE unmoved (`check-fingerprints --mint` reports 4/4 re-minted, 0 stray copies at
every step). But `npm run verify -- --premerge` finished at 844 s with **PASS 34 FAIL 1** — the
one failure is `viewer-invariants` reporting **17 crossing violation(s)** in 2 races on
`city-circuit` seed 9 and `space-sprint` seed 9. Both winners finish on canvas at PHOTO_FINISH,
band% 100 / 70.65, corridors 1.62 / 0.71; the seven per-invariant counters (course, leader,
line, widthstep, panstep, tootight, toowide) are all **0 frame(s) in 0 race(s)**.

★★ **A LIKELY EXPLANATION, RECORDED SO HE CAN VERIFY IT.** Memory carries a rule that reads
*"item 10 FAILs on purpose"* (GATE-WIRED-AND-CAUSED-1, and `viewer-invariants.mjs:179` — garden-
path passes ten items and fails two, item 9 and item 10). If the 17 crossing violations here are
those accepted-by-owner cases, this is the pre-existing state that pre-commit `verify` hides and
`verify --premerge` surfaces. **This run's changes cannot have caused it**: the racer-types move
was byte-preserving and the fingerprints did not move — so the same 17 events must be firing on
master. I did NOT verify that against master because the check takes 15+ minutes; that is what
NEEDS HIS EYE first.

**Recorded plainly, not argued away.** The safest reading is: do NOT merge without his check.
Everything my run built is on the branch and pushed; the merge is his to authorise once the
viewer-invariants state is confirmed as the accepted one.

---

## Source hygiene

- **Lines before/after**: hard to state at file level given PIECE 5's 122-file diff (199+/199−, a
  pure rename shift), but each other piece was under 100 net-new lines in code and roughly the
  same again in tests.
- **What was reused**: `InfoTooltip` (existing), `frameCameraInputs()` (existing), `migrateTeams`
  (existing — the ledger imports it rather than re-implementing). The RangeSlider is the only new
  component and it deliberately preserves the caller's own `<span>` for display, so nothing
  visible about the surrounding sections changed.
- **What was removed**: no dead code deleted. PIECE 5 moved 80 files; no delete.
- **Noticed but left**: the SurfaceClassManager `ConfigFields` sub-component and its schema-driven
  fields carry a lot of styling; a wider refactor was not this piece's brief and was not done. The
  `perfLog.js` file has a `camera:` object literal (perf timing, unrelated); the guard's marker
  set was chosen to not collide with it.
