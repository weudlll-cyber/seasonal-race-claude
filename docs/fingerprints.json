{
  "_": [
    "THE ONE HOME for the three current fingerprints. No document carries a copy — not even a",
    "generated one. Other documents REFERENCE this file, or say nothing.",
    "",
    "ONE-TRUTH-1 made 19 copies machine-written and machine-checked. That was the wrong shape: a",
    "generated copy is still a copy, it still has to be kept in step, and a reader who finds one has",
    "no way to know whether it is the truth or a stale rendering of it. ONE-TRUTH-2 (the owner's",
    "order) deleted all 19. Where a document's argument needed the reader to know a fingerprint",
    "EXISTS, it now points here; where it did not, the mention was removed.",
    "",
    "WHERE A CURRENT VALUE MAY STILL APPEAR, and nowhere else:",
    "  - this file",
    "  - docs/TAGS.md — the tag register. A tag's hash is what it was when the tag was cut.",
    "  - reports/ — the lab journal. Append-only by rule: a report records what was true on the day",
    "    it was written and must never be rewritten. Listed as a PREFIX rather than 112 filenames,",
    "    which is a decision: enumerating them would be noise that nobody would maintain, and the",
    "    append-only rule already makes every one of them history by construction.",
    "",
    "TO RE-MINT: run the role's `reproduce` command, put the new value here, and commit. There is",
    "nothing else to update — that is the point.",
    "",
    "WHICH DOCUMENT OWNS WHAT, resolved because four of them implied they owned this:",
    "  - THIS FILE owns the CURRENT VALUES. Nothing else does.",
    "  - docs/SIM.md owns the LINEAGE — which change moved which value to which. Its head points here.",
    "  - reports/parity/REBASELINE.md owns WHAT MOVED each one, and now carries no values.",
    "  - docs/SHIP-CEREMONY.md owns the PROCEDURE for minting."
  ],
  "roles": {
    "world": {
      "value": "dc4647be0f55ebdb",
      "covers": "the RACE: physics, plan, outcome",
      "reproduce": "node scripts/fingerprint-default.mjs",
      "mintedOn": "3518ee0b",
      "mintedBy": "RACER-MOTION-2 — ship lateral acceleration cap (maxLateralAccelPerStep 0.0005)",
      "date": "2026-07-31",
      "lastVerified": "2026-08-17 — NOT re-run at SHIP-RUNIN-1, and that is the stronger answer rather than a skipped step: walking scripts/lib/routing.mjs closureOf from this instrument's declared reach entries gives 36 files, and NONE of the 17 files the merge puts on master is one of them. The ship's only production file is CameraDirector.js, which is outside this closure and inside CAMERA's and RENDER's. engine-reach was deliberately not leaned on: on a committed merge it compares the working tree and can say nothing about the merge. Previously 2026-08-12 — re-run in full on eea0acf2 (the RUNIN-1 merge) and UNMOVED. Run rather than argued from engine-reach: defaults.js IS in the engine hull and this merge genuinely edits it, and engine-reach --check compares the WORKING TREE, so on a tree whose merge is already committed it reports byte-identical and can say nothing about the merge. The two keys it adds are camera keys, which is why the race is untouched — but that is the conclusion the measurement supports, not the reason to skip it. Previously 2026-08-12 — re-run at CLEAN-STATE-1."
    },
    "camera": {
      "value": "6ae77f12daf23f78",
      "covers": "the DIRECTOR's decisions: state, phase, anchor, zoom, offsets, camT, targets — INCLUDING the ending the director composes after the last crossing (CAMERA-ENDING-WINDOW-1)",
      "reproduce": "node scripts/camera-fingerprint.mjs --quiet",
      "mintedOn": "48f954a4",
      "mintedBy": "SHIP-RUNIN-1 — THE RUN-IN HOLDS THE OPENING SHOT AND CLOSES ONCE, AND THE FINISH LINE STAYS IN FRAME. THE OWNER JUDGED THIS ON A PRODUCTION BUILD ON 2026-08-17 AND ACCEPTED IT; this hash is that acceptance written down. THREE CHANGES MOVE IT AND ALL THREE ARE FRAMING, NOT STATE: (1) RUNIN-LINE-1 — the corridor cap no longer overrides the line's own ceiling. `_setTargets` honoured `_ceilings.line` in its `Math.min` and then let the corridor cap raise the composed zoom back above it, so the one term that knows where the finish line is was being overruled by a term that does not. The repair is one clause, and an instrument was built BEFORE it rather than after: `check-runin-frame` gained a THIRD question — is the line in frame from the endgame threshold to the crossing, on ten tracks — which splits its losses by cause (overridden vs merely trailed) so a future regression names itself. (2) RUNIN-HOLD-1 — the opening shot is HELD and then closed in ONE sweep. Left alone `_lineCeiling` begins closing on the frame the window opens, which measured about 3.6 s of lead-in at roughly 95 px/s of picture flow, below the rate at which anything reads as movement. The release is DERIVED, not chosen: the sweep lasts `runInOpenMs`, so it begins when the leader's remaining time to the line — measured over the RUN-IN's own span, because the field decelerates into the finish and a whole-race average was wrong by about six times — has fallen to that length. No key was added. (3) RUNIN-BACK-1 — the leader's placement is the owner's again, a little BEFORE the centre of frame easing to a little AFTER it, and RUNIN-AHEAD-1's forward bound, which contradicted that specification, is removed with nothing in its place: placing the leader behind centre is ITSELF the reason the frame does not reach past the line. It narrowed nothing — WHY-SO-WIDE-1 measured that the extra width the bound bought carried no racer, the whole field spanning 600-830 px inside a frame the line was forcing to 2668. DEFAULTS ARE NOT TOUCHED: `endgameThreshold` keeps its shipped value and the Dev control merely steps it in 1% instead of 5%. WHY WORLD AND WORLD-OFF CANNOT MOVE, decided by the instruments' own declarations rather than asserted: walking `scripts/lib/routing.mjs` `closureOf` from each declared reach entry, NONE of the 17 merged files lies inside `fingerprint-default.mjs`'s closure (36 files), while both this instrument's closure (36) and RENDER's (55) contain `CameraDirector.js` — the single production file this ship changes. `engine-reach` was deliberately NOT leaned on: on a committed merge it has no working-tree diff to read and its verdict would not be evidence. FIVE FURTHER SHAPES WERE BUILT, MEASURED AND REVERTED on the branch and none is in this tree — RUNIN-PIN-1, RUNIN-ANCHOR-1, RUNIN-RATE-1, RUNIN-EVEN-1/-2 and RUNIN-SCHEDULE-1. Replaces ff2bc42af377b5cf (CONTENDER-ZOOM-1).",
      "date": "2026-08-17",
      "lastVerified": "2026-08-17 — measured fresh on the merge commit 48f954a4 by node scripts/camera-fingerprint.mjs --quiet (no --cheap), and again by npm run verify on the same tree. WORLD and WORLD-OFF were NOT re-run: no merged file lies inside fingerprint-default.mjs's declared closure, so they cannot move.",
      "supersededMintedBy": "CONTENDER-ZOOM-1 — THE PHOTO FINISH FRAMES EVERYONE STILL ABREAST, and the shot is never wider than the road. The framed set stops being ordered.slice(0, 2): a racer is a contender when he is BOTH nearly level with the leader AND on a free lane, because a racer on the same lane as somebody ahead cannot win — he would have to move aside and then still overtake, and the shot is far too short for both. NEITHER CONDITION IS A NEW NUMBER: both are the engine’s own geometry from pairContact in raceBehavior.js — contactLength (one body length between equal racers) for level, contactWidth with rowLayout’s physicalY unit for the lane. The race is lane-free and physicalY is continuous, so “same lane” had to be geometric. DETECTION AND DURATION ARE UNCHANGED: the gate still enters on the top two, and _photoFinishContendersHome still reads the leading two — letting the framed set decide when the shot ENDS stretched the photo finish by 85% and produced 59 empty frames. THE CORRIDOR CAP SHIPS AS PART OF THE SAME FEATURE and is not separable: priced apart it costs nothing (3.4% contenders-not-whole with it nulled against 3.4% with it arriving, empty frames 46 -> 35), so a second key would buy a switch nobody has a reason to throw. THE CAP ARRIVES OVER corridorCapArriveMs RATHER THAN APPEARING: its scope is a state predicate, which is a cut, and it used to switch on in one frame and take the target 2.47 -> 10.02 — the whole of the leap the owner objected to. Hanging it on the run-in’s continuous progress was built first and FAILED, because the cap then escaped the finish shot and tightened OVERVIEW from 1.5 corridors to 0.469. MEASURED on ten tracks x three seeds: contenders not whole 10.3% -> 3.4%, crossing zoom median 99%, photo-finish frames 7468 against master’s 7441, check-runin-frame green on both halves. WORLD is UNMOVED and was RUN rather than argued. Replaces d7a8fe54072df6d7 (CAMERA-ENDING-WINDOW-1). Superseded note: CAMERA-ENDING-WINDOW-1 — THE INSTRUMENT CHANGED, NOT THE PRODUCT, and that is why this move is recorded rather than avoided. NOT ONE LINE OF CAMERA BEHAVIOUR IS TOUCHED: the diff is the harness's stop condition, one new pure function in endingSchedule.js, the call site that already computed that same arithmetic inline, and three comments that had gone false. WHAT WAS WRONG: the instrument ran `while (finishedCount < N)`, so it stopped on the exact frame the ending BEGINS and rendered no FINISHED frame at all. ENDING-PICTURE-1 — which makes RaceScreen keep consulting the director through PHASE.FINISHED so an in-flight zoom-out can come to rest — was therefore INVISIBLE to the camera's own change detector, and defaults.js stated that in writing as if it were a property rather than a hole. THE WINDOW IS NOW DERIVED, NOT TYPED: `endingOnRaceScreenMs()` is the same function RaceScreen sets its navigate-away timer from, so a change that lengthens the ending lengthens this window with it. That is the requirement — a fixed condition can be silently outgrown, and this one was. THE OFF-ARM PROMISE IS EXACT AND WAS MEASURED: `--ending-off` turns `endingKeepsFinishShot` false, the arm in which the director does not compose the ending, and the instrument reproduces c1556053b1824758 BYTE-IDENTICALLY — so every frame before the last crossing is untouched and the whole move is the ending. PROVEN BY SABOTAGE: a perturbation firing only from the SECOND all-home frame onward moves this hash to f53bbf036f93f28a while leaving the --ending-off control at c1556053b1824758; removing it returns the hash to d7a8fe54072df6d7. 9 of 10 tracks contribute 300 FINISHED frames each, and the instrument now PRINTS that count and refuses to run if no track contributes any, so the blindness cannot return quietly. garden-path does not finish inside the harness's 200 s ceiling and has no ending to sample. WORLD (dc4647be0f55ebdb) and RENDER (c962df5334277f95) were RE-RUN IN FULL on this tree and are UNMOVED. Replaces c1556053b1824758 (RUNIN-1).",
      "supersededDate": "2026-08-14",
      "supersededOn": "0bd07dba",
      "supersededNote": "ff2bc42af377b5cf was CONTENDER-ZOOM-1's value, minted on 0bd07dba. It is the last state of this hash before the run-in held its opening shot."
    },
    "render": {
      "value": "a870f5f9e79cb444",
      "covers": "the DRAW CALL SEQUENCE: sprite placement, text, styles, transforms, layer order",
      "reproduce": "node scripts/render-fingerprint.mjs --quiet",
      "mintedOn": "48f954a4",
      "mintedBy": "SHIP-RUNIN-1 — the same ship as CAMERA above, and this hash moves for the reason that one does: the run-in composes a different shot through the whole endgame, so the transform on every drawn frame of that window changes. NOTHING IN THE DRAWING ITSELF CHANGED — no sprite, no text, no layer order, and no file under `renderRaceFrame.js` is in the diff. The move is entirely the camera's, and it is visible here only because RENDER's declared closure (55 files) contains `CameraDirector.js`. WORLD and WORLD-OFF were NOT re-run: no merged file lies inside `fingerprint-default.mjs`'s computed closure, which is a stronger statement than a matching hash and is the one the closure can make. Replaces 0e04fa4a5e9c3b85 (MINIMAP-MARKS-1 + MINIMAP-TAIL-1).",
      "date": "2026-08-17",
      "lastVerified": "2026-08-17 — measured fresh on the merge commit 48f954a4 by node scripts/render-fingerprint.mjs --quiet (no --cheap), and again by npm run verify on the same tree.",
      "supersededMintedBy": "MINIMAP-MARKS-1 + MINIMAP-TAIL-1 — THE MINIMAP SAYS WHERE THE RACE STARTS, WHERE IT ENDS, AND WHAT IS NEVER RACED. Two blocks, one ship, because master gets a visible change once. THE OWNER JUDGED BOTH ON A PRODUCTION BUILD ON 2026-08-15 AND ACCEPTED THEM; this hash is that acceptance written down. WHAT MOVED IS DRAWING AND NOTHING ELSE: the minimap gains a start mark, a finish mark, and a wash over the stretch behind the finish, all inside the same canvas the sampler hashes. THE MARKS ARE BARS ACROSS THE BAND at getPosition(t, +-0.5) — the SAME segment the world finish gate spans, since drawOpenTrackFinishLine extrudes getPosition(ft, 0) by openTrackHW = trackWidthPx / 2 = the width getPosition offsets by — so the mark and the line the racers cross cannot drift apart by construction rather than by agreement. Solid green starts, a black/white checker finishes, and where the two coincide (every closed track, where the gate and the front start row both sit at t 0) ONE mark carries both: the checker on a green plate. SIZES WERE CHOSEN AGAINST A MEASUREMENT, NOT A GUESS: the bar is 12-22 panel px on the ten shipped tracks and its ends land within 1.5 panel px of the drawn band edge, both measured across all ten; an earlier reading assumed about 50 px and would have drawn a blob on half the game. THE UNRACED TAIL IS BUILT FROM THE MARKS OWN SOURCE, which is the whole reason its seam lands on the checker: driving the shipped renderMinimap through a recording context on all ten tracks at four finish positions gives SEAM 0.000000 panel px, every open track, every finish. The alternative — the bands own index-paired getEdgePoints — was rejected with numbers: those two parameterisations disagree by up to 502 world px on luger-hill, an along-track offset that would have put the seam visibly off the mark. The tails side boundary is a different question and is NOT zero: it washes a band drawn from the other source, so it sits up to 1.74 panel px off that bands outline, below the width of the cyan edge that covers it. THE WASH IS DRAWN BEFORE THE EDGE OUTLINES so the cyan edge still runs through it — without that the tail reads as the track ends here, which is the opposite of true. NO CONFIG KEY WAS ADDED and the minimap still reads nothing but the shape it is handed; the two t values come in as parameters. WHY ONLY RENDER MOVES, and it was decided by the guards own declarations rather than asserted: walking scripts/lib/routing.mjs closureOf from each instruments declared reach entry points, NONE of the six merged files lies inside WORLD/WORLD-OFF (36 files) or CAMERA (36 files), while RENDER (55 files) contains both Minimap.js and renderRaceFrame.js. engine-reach was deliberately NOT leaned on here: on a committed merge it has no working-tree diff to read and its verdict would not be evidence. Replaces 0d5854a652c69d87 (CONTENDER-ZOOM-1).",
      "supersededDate": "2026-08-15",
      "supersededOn": "8a2dacab",
      "supersededNote": "0e04fa4a5e9c3b85 was MINIMAP-MARKS-1 + MINIMAP-TAIL-1's value, minted on 8a2dacab. It is the last state of this hash before the run-in changed the endgame's transform."
    },
    "world-off": {
      "value": "854018ee5d3d83e1",
      "covers": "the world with gap-reroll OFF — the pre-feature invariant that proves the shipped change is the only thing that moved",
      "reproduce": "node scripts/fingerprint-default.mjs off --gapRerollEnabled=false",
      "mintedOn": "3518ee0b",
      "mintedBy": "RACER-MOTION-2",
      "date": "2026-07-31",
      "lastVerified": "2026-08-17 — NOT re-run at SHIP-RUNIN-1, for the same closure reason as the ON world above: this is the same instrument under a flag, so it has the same 36-file closure and the same answer. Previously 2026-08-09 — re-run at SHIP-ROUTING and UNCHANGED, so the pre-feature invariant holds either side of this ship too. SHIP-THREE did not re-run it: nothing in that ship touches the engine (engine-reach clears every changed path except defaults.js, whose only moved key is read by the camera), and the ON world is unchanged, so the OFF arm has no question to answer. Previously re-run at the SHIP-THE-LINE merge and UNCHANGED, so the pre-feature invariant still holds either side of that ship. Previously 2026-08-06 — RESOLVED and VERIFIED by running it. This was UNKNOWN in ONE-TRUTH-1. docs/SIM.md's lineage table names the flag; the first command written here OMITTED THE LABEL argument, argv[2] swallowed the flag as a label, and the run returned the ON value while printing 'default config'. The label is mandatory. With it, this value reproduces exactly. Re-run at RENDER-SAMPLER-CEREMONY on 0308f7b9 and UNCHANGED, measured rather than argued because the ON world was measured too."
    }
  },
  "historyHomes": [
    "docs/TAGS.md",
    "reports/"
  ],
  "machineExceptions": [],
  "_machineExceptionsNote": [
    "Files that must contain a current fingerprint as a LITERAL because something reads it — a test,",
    "a baseline, a script. Listed BY NAME, never by pattern, each with its reason.",
    "",
    "The list is EMPTY, and that was measured rather than assumed: every tracked file was scanned and",
    "not one test, script or config consumes a fingerprint string. All 19 former copies were prose or",
    "comments meant for a human. If you are about to add one, make it read this file instead."
  ]
}
