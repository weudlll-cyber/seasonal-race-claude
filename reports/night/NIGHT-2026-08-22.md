# NIGHT-2026-08-22 — the morning sheet

**Base:** master `2c1122e2`. **Updated after every piece, in its own commit on master.** If the
night is stopped at any hour, this file is still a true picture.

**How to read it:** four questions, in this order — what is DONE and MERGED · what is RUNNING now ·
what is LEFT in the chain · what needs YOUR WORD before anyone can proceed.

**Nothing tonight has touched the race.** No piece so far has changed a shipped source file, a
default, or a drawn frame. No fingerprint has been selected by routing, because none could have
moved.

---

## 1 — DONE AND MERGED

### PIECE 1 — the ceremony-skip handler really is on the wrapper · merge `5e6b61ff` · CI green

CEREMONY-SKIP-2 shipped saying one line was unproven: that `RaceScreen` attaches the handler to the
element it claims. **It does, and it is now proven** — a source-reading test with four assertions,
because the claim has four ways of going wrong and three of them leave the naive check green. All
four were made to fail by sabotaging the real `RaceScreen/index.jsx` and watching red.

**What it also produced, and it is the part worth your attention:** a BACKLOG finding that
**`RaceScreen` is not testable.** 1907 lines; first paint returns a placeholder until an effect
reads `sessionStorage`; the race is built in a second effect that needs a canvas and a geometry; an
rAF draw loop. **The sharpest evidence is that the only two test files naming it name it to AVOID
it** — `App.test.jsx` mocks it to `() => null`, and `buildIdentitySource.test.js` reads it as text.
Recorded with evidence, no proposal, as ordered. Report:
[CEREMONY-SKIP-WRAPPER-1](../evolution/CEREMONY-SKIP-WRAPPER-1.md).

### PIECE 2 — the dead diagnostic test is armed · merge `c89d09ce`

`trackCorridor.test.js:61` had closed with `expect(true).toBe(true)` since 2026-05-06 — the only
such assertion in the tree. **It was armed rather than deleted, and the reason is a finding:**
nothing else in the client suite reads the committed track geometries, so a corrupt or width-less
seed passes the whole suite today. Its two units are covered thoroughly elsewhere; the ten real
geometries are covered nowhere. Four tests now replace the printed table, four sabotages red.

**AND IT HAD BEEN READING THE WRONG WIDTH.** The old table called `getActualTrackWidth()`
unconditionally, while all **seventeen** shipped call sites read `geo.width ?? …`. Measured over all
ten tracks, the spline estimate **overestimates on every one of them** (1.025–1.497), not only on
open tracks as two shipped comments state — closed tracks run up to **+12.8%**. That correction is
proposed, not taken: it is a comment on shipped source in two trees and this piece was scoped to a
test file. Report: [CORRIDOR-DIAG-ARMED-1](../evolution/CORRIDOR-DIAG-ARMED-1.md).

### PIECE 3 — `ci.yml` declares its token scope · merge `6cfbae1a` · CI green on the PR FIRST

The only item the VPS list calls a MUST. `ci.yml` had no `permissions:` block at any level; it now
declares `contents: read`, read off the three jobs rather than copied from a template — only
`actions/checkout` and `check-tags.mjs`'s `git ls-remote` touch the token, and nothing in the
workflow writes.

**I overstated the fix in the first draft and corrected it before committing.** Your repository's
`default_workflow_permissions` is **already `read`**, so no write token was ever exposed and nothing
was at risk. What the block genuinely buys is narrower and is now what the file says: a `read`
*default* means read on **every** scope, and an explicit block makes the eleven it does not name
`none`; and the default is a repository *setting* an admin can flip in the UI with no diff to show
for it. Report: [CI-PERMISSIONS-1](../evolution/CI-PERMISSIONS-1.md).

### PIECE 4 — `engine-reach --check` refuses when it was asked nothing · merge `465d49c5` · CI green

**The two answers had nothing to do with the diff base, staging, or the working directory.**
`--check` reads **no tree at all** — it answers about the paths on its command line. `none of 0
path(s)` was the literal empty-argument case, printed with **exit 1, which reads as a licence to skip
a fingerprint**. The pre-commit hook can never provoke it because it guards its call; a hand-typed
command substitution that expands empty can.

Now: **exit 2, stdout empty** — for an empty path list and for a `--base=` that does not resolve.
A second false clearance was fixed alongside: the negative message conflated *not in the hull* with
*in the hull but unchanged*, so the tool said `defaults.js` "cannot reach the race engine" about a
file that can reach it from anywhere.

**One over-reach I built was caught and removed before commit** — a base==HEAD refusal that would
have fired on ordinary uncommitted work and **silenced the mint tripwire on every first commit of a
branch**. Proving the *working* direction is what caught it. Report:
[REACH-REFUSES-1](../evolution/REACH-REFUSES-1.md).

---

## 2 — RUNNING NOW

**PIECE 5** — make the backlog honest. The census, a verdict per item, the edits, and the three
buckets that decide the rest of the night. The largest piece in the chain.

**Your correction is in force:** the four-minute-per-item ceiling is withdrawn. **No item in this run
was ever decided by a clock** — PIECE 5 had not started when the correction arrived, so there is
nothing to re-decide. An item is finished when the evidence decides it; CANNOT ESTABLISH now means
the evidence does not settle it and never that time ran out, and each such verdict names the one
command or measurement that would.

---

## 3 — LEFT IN THE CHAIN

| #   | piece                             | one line                                                             |
| --- | --------------------------------- | -------------------------------------------------------------------- |
| 5   | make the backlog honest           | **running** — census, verdicts, edits, and the three buckets          |
| 6   | candidate lists → verified numbers | verify only, delete nothing                                          |
| 7   | two duplicate truths              | one home each; every fingerprint reached must come back byte-identical |
| 8   | the fairness split                | read-only: how much of band arrival is the draw, how much the regulator |
| 9   | `reports/perf` inventory          | inventory + a retention PROPOSAL; delete nothing, move nothing        |
| 10  | `_lfEntryByState` explained       | read-only; ends as one question for you                               |
| 11  | `city-circuit` three curves       | diagnose only, do not repair                                          |
| 12  | `d ln(width)/dt`                  | build the instrument; wire it into nothing                            |
| 13  | work the survivors                | bucket (i) from piece 5, smallest first                               |

---

## 4 — NEEDS YOUR WORD

**Nothing is blocked on you yet.** Three items are queued for your decision; none stops the chain.

### a. The two shipped comments about the corridor-width overestimate are imprecise. Correct them?

`RaceScreen/index.jsx:427` and `sim-fairness.mjs:4306` both say `getActualTrackWidth()` overestimates
**for open tracks**. Measured tonight: it overestimates on **all ten**, closed ones by up to 12.8%.
Anyone reasoning about the fallback on a closed track reads a comment implying it is exact there.

**Your question: should the correction ride along with whoever next touches those lines, or is it
worth a piece of its own?** It is two comment lines in two trees and cannot move a number, but one
of them is on the sim-parity path.

### b. `RaceScreen` is not testable — recorded, with nothing proposed.

The finding is in `docs/BACKLOG.md` with its evidence. **No rewrite is implied and none was
proposed**, as ordered. **Your question: is this worth doing anything about at all**, or is
source-reading coverage of that one file an acceptable permanent state?
### c. `deploy.yml.disabled` has no `permissions:` block either, and was deliberately left.

It is disabled by its extension, so no run can be produced from it and **CI can never exercise an
edit to it** — which is why PIECE 3 did not touch it. **Your question: should it get a block now, on
the understanding that the change is unverifiable until the day it is re-enabled, or does that
belong to whoever re-enables it?**


---

## THE NIGHT'S OWN RULES, so you can check I kept them

- One piece at a time, each on its own branch off master, merged before the next begins.
  **Master is in a finished state after every piece.** Branches deleted at origin after merging;
  `git ls-remote --heads origin` returns master alone.
- R15 governs every check: a check runs only if this change could have altered its answer. Every
  report names what it skipped and what already determined the answer.
- Nothing in bucket (ii) or (iii) is pulled forward because the night has time left. **Time is not
  authority.**

### One defect of my own, recorded rather than left to be found

The morning-sheet commit `078de460` added this file without its `reports/night/INDEX.md` line, so
`check-index` went red on master the moment it landed. The pre-commit hook does not run that guard;
`npm run verify` on the first piece's branch found it. Repaired in `336ec7b3` and merged with
PIECE 1. Master was red on that one guard for roughly forty minutes.
