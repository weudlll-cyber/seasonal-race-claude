# NIGHT-2026-08-22 — the morning sheet

**Base:** master `2c1122e2`. **Written first, updated after every piece, in its own commit on
master.** If the night is stopped at any hour, this file is still a true picture.

**How to read it:** four questions, answered in this order — what is DONE and MERGED · what is
RUNNING now · what is LEFT in the chain · what needs YOUR WORD before anyone can proceed.

---

## 1 — DONE AND MERGED

*(nothing yet — the night has just started)*

---

## 2 — RUNNING NOW

**PIECE 1** — close the one unproven line of CEREMONY-SKIP: that `RaceScreen` attaches the
handler to the wrapper.

---

## 3 — LEFT IN THE CHAIN

| # | piece | one line |
| --- | --- | --- |
| 1 | ceremony-skip: the wrapper line | prove the handler is attached to `.race-canvas-wrapper` |
| 2 | the dead diagnostic test | `trackCorridor.test.js:61` asserts `true` — delete or arm it |
| 3 | `ci.yml` has no `permissions:` | the only MUST on the VPS list |
| 4 | `engine-reach --check` two answers | a tool that cannot see the diff must REFUSE |
| 5 | make the backlog honest | census, verdicts, edits, and the three buckets |
| 6 | candidate lists → verified numbers | verify only, delete nothing |
| 7 | two duplicate truths | one home each; every fingerprint must come back byte-identical |
| 8 | the fairness split | read-only measurement: the draw versus the regulator |
| 9 | `reports/perf` inventory | inventory + a retention PROPOSAL; delete nothing |
| 10 | `_lfEntryByState` explained | read-only; one question for you at the end |
| 11 | `city-circuit` three curves | diagnose only, do not repair |
| 12 | `d ln(width)/dt` | build the instrument; wire it into nothing |
| 13 | work the survivors | bucket (i) from piece 5, smallest first |

---

## 4 — NEEDS YOUR WORD

*(nothing yet)*

---

## THE NIGHT'S OWN RULES, so you can check I kept them

- One piece at a time, each on its own branch off master, merged before the next begins.
  **Master is in a finished state after every piece.**
- R15 governs every check: a check runs only if this change could have altered its answer.
  Every report names what it skipped and what already determined the answer.
- Nothing in bucket (ii) or (iii) is pulled forward because the night has time left.
  **Time is not authority.**
