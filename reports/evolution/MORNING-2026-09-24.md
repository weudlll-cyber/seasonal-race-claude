# MORNING 2026-09-24 — what is actually open, verified at the tree

**Branch `docs/open-truth-2026-09-24`, off master `1b47b5a3`. Documentation only.**

---

## DONE

**PIECE 1 — HARVEST.** Every place an open item can hide, swept ungapped over the whole tree.

- ★★ **TWO WHOLE CANDIDATE CLASSES ARE EMPTY, and that is a result rather than a gap in the
  sweep.** `TODO`/`FIXME`/`HACK`/`XXX` case-insensitive over every tracked file: **18 hits, none of
  them a marker in source** — 9 in `reports/`, 3 in `docs/` (all prose *about* past cleanups), 2 in
  a server test as password fixtures (`pass1-xxx`), and the rest historical. **There is no TODO in
  this codebase.** Likewise **no skipped or focused test anywhere**: no `.skip`, `.todo`, `xit`,
  `xdescribe`, `skipIf` or `.only` in `client/`, `server/`, `scripts/` or `shared/`; every "skip"
  hit is prose in a comment.
- **Candidates gathered** from `docs/OPEN.md` (41 entries), `docs/BACKLOG.md` (the 3 of 7 "his" items
  still standing), `docs/DEPLOY-NOTES.md`, `docs/SIM.md`, `docs/MORNING.md`, `docs/SHIP-CEREMONY.md`,
  `docs/DEAD-ENDS.md` (§A–§T), `docs/TAGS.md`, and 139 open-state lines across 34 files in
  `reports/` (read, never edited).

**PIECE 2 — first verified batch, and the failure mode the night was commissioned for is real.**

- ★★★ **THREE OF THE FOUR "UNMERGED CAMERA BRANCHES, HIS EYE OWED" ARE ALREADY IN MASTER.**
  `239644aa` (resolve-converge) came in by `d7eca25d`, `e91e7a61` (run-in state) by `eea0acf2`,
  `84b7c8f0` (finish-readable) by `69e4b27b`. Only `87a08af4` (front-group) is not — and it is
  **preserved as tag `archive/front-group`**, so nothing is at risk. Established with
  `git merge-base --is-ancestor`, not from any list.
- ★★ **`docs/MORNING.md` says two branches are "left at origin" carrying undecided product code.
  Origin has ONLY master.** `feat/remove-prestaging-comebacker` is preserved as
  `archive/remove-prestaging-comebacker`; `night/2026-09-14-history` is at neither origin nor any
  archive tag.
- ★★ **CORRECTION TO THIS NIGHT'S OWN BRIEF, and it makes the gap larger, not smaller.** The brief
  says the production browser arm "runs no CI (`ci.yml` names it only in a comment)". **`playwright`
  appears NOWHERE in `.github/` at all** — not in a comment, not in any workflow. CI has exactly
  three jobs (Client checks, Server tests, Living-doc guards + script tests). **No browser arm runs
  in CI, dev or production.**

**PIECE 2 — VERIFIED AT THE TREE. 58 candidates, and roughly three in ten were not open.**
**41 STILL OPEN · 9 ALREADY DONE · 8 PREMISE GONE.** Full list with evidence in
[docs/OPEN.md](../../docs/OPEN.md). Beyond the branch findings above: *"why Garden Path does not
finish"* — **it finishes**, all 60 races measured 2026-09-23; *"the definitive N=300"* — **run**;
*"nothing measures MOTION"* — **closed** by FINISH-MOTION-1, whose own stage 2 is in master by
`e2ad9cfd` (*OWNER-APPROVED*) while its report still says it awaits his eye.

**FOUR LIVING DOCUMENTS CORRECTED IN THE SAME PIECE**, per the standing rule of 2026-09-19:
`docs/OPEN.md` (replaced), `docs/MORNING.md` (the two branches), `docs/BACKLOG.md` (the racer-config
move is **80 files, not 39** — counted), and `docs/DEPLOYMENT.md`. **`reports/` was read and never
edited.**

**PIECE 3 — HOW FAR TO "DONE".**

- ★★ **A CLEAN CHECKOUT INSTALLS, BUILDS AND RUNS — the known native-module risk did not
  materialise.** Done rather than reasoned: fresh clone to `/c/tmp/ra-clean`, `npm ci` in both trees
  (exit 0 each), `npm run build` (3.71 s), `node server/src/index.js` — **booted on 4399 and served
  the built client**; `bcrypt` and `better-sqlite3` both `require()` successfully. ★ **One machine
  only**: same Windows, same Node 24.14.0, same toolchain — a Linux or ARM host is NOT tested.
- ★ **A real defect found in `docs/DEPLOYMENT.md` and fixed.** It set `RA_BOOTSTRAP_TOKEN` as a
  per-command prefix to `node`, then used `$RA_BOOTSTRAP_TOKEN` in a fresh shell where it is empty —
  so the printed setup `curl` sent an empty token. It also never said to install dependencies at all.
- **No BACKUP procedure and no UPGRADE path** for an installed instance — zero hits in
  `DEPLOYMENT.md`. Both sized as a block; what a minimal one must cover is stated, not built.
- **The licence is SETTLED** (`AGPL-3.0-or-later`, his answer of 2026-09-01) and **`docs/AUTH.md` has
  no open decision** across its nine sections.

**PIECE 4 — `docs/OPEN.md` REPLACED**, sorted by what he has to do with it, with the scoreboard.

---

## RUNNING

Nothing. This night runs no measurement — it reads the tree.

---

## OPEN

All four pieces are done. What stays open is the WORK the night catalogued, not the night:
**41 verified-open items** in [docs/OPEN.md](../../docs/OPEN.md), of which the delivery gaps are the
ones that stand between today and someone else using this — **no backup procedure, no upgrade path,
no browser test in CI, and no public address.**

---

## NEEDS HIS WORD

**Twelve items, verified to be genuinely his** — section 1 of [docs/OPEN.md](../../docs/OPEN.md).
The two cheapest and newest: **which cut counts as "the leading group"**, and **whether a group of
TWO at the front is a breakaway he objects to** (he ruled on three and on more-than-four; the pair
case has never been ruled on and must not be interpolated).

★ And one that is his word plus a purchase, because it blocks everything in section 5:
**a domain name.** Without it there is no HTTPS, and without HTTPS sign-in does not work at all.
