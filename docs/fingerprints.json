{
  "_": [
    "THE ONE HOME for the three current fingerprints. No document carries a copy \u2014 not even a",
    "generated one. Other documents REFERENCE this file, or say nothing.",
    "",
    "ONE-TRUTH-1 made 19 copies machine-written and machine-checked. That was the wrong shape: a",
    "generated copy is still a copy, it still has to be kept in step, and a reader who finds one has",
    "no way to know whether it is the truth or a stale rendering of it. ONE-TRUTH-2 (the owner's",
    "order) deleted all 19. Where a document's argument needed the reader to know a fingerprint",
    "EXISTS, it now points here; where it did not, the mention was removed.",
    "",
    "WHERE A CURRENT VALUE MAY STILL APPEAR, and nowhere else:",
    "  - this file",
    "  - docs/TAGS.md \u2014 the tag register. A tag's hash is what it was when the tag was cut.",
    "  - reports/ \u2014 the lab journal. Append-only by rule: a report records what was true on the day",
    "    it was written and must never be rewritten. Listed as a PREFIX rather than 112 filenames,",
    "    which is a decision: enumerating them would be noise that nobody would maintain, and the",
    "    append-only rule already makes every one of them history by construction.",
    "",
    "TO RE-MINT: run the role's `reproduce` command, put the new value here, and commit. There is",
    "nothing else to update \u2014 that is the point.",
    "",
    "WHICH DOCUMENT OWNS WHAT, resolved because four of them implied they owned this:",
    "  - THIS FILE owns the CURRENT VALUES. Nothing else does.",
    "  - docs/SIM.md owns the LINEAGE \u2014 which change moved which value to which. Its head points here.",
    "  - reports/parity/REBASELINE.md owns WHAT MOVED each one, and now carries no values.",
    "  - docs/SHIP-CEREMONY.md owns the PROCEDURE for minting."
  ],
  "roles": {
    "world": {
      "value": "8a1977187e9c99b4",
      "covers": "the RACE: physics, plan, outcome",
      "reproduce": "node scripts/fingerprint-default.mjs",
      "mintedOn": "fa553f50",
      "mintedBy": "FINGERPRINT-TRACK-DEFAULTS-1 \u2014 AN INSTRUMENT CORRECTION. THE OWNER GRANTED MINTING PERMISSION ON 2026-09-02 FOR THIS MINT AND NO OTHER, on the reasoning that the picture does not change \u2014 the instrument does \u2014 so there is nothing for his eye to judge.\n\n\u2605 THIS IS NOT A WORLD CHANGE, AND A READER IN SIX MONTHS MUST NOT CONCLUDE THAT IT IS. The simulation is untouched: the diff contains no engine file, no default, no config, and no seed. SHIP-CEREMONY's world-changing half \u2014 REBASELINE, the SIM lineage entry for a moved world, the golden re-pin \u2014 exists for the case where the RACE moved, and it does NOT apply here. What moved is which race the instrument runs.\n\nWHAT WAS WRONG. `scripts/fingerprint-default.mjs` carried a literal table of ten track/racer pairs under a comment reading '10 standard tracks x default racer'. That was true when written and false from 2026-08-25, when GARDEN-PATH-DEFAULTS-1 changed that track's defaultRacerTypeId from snail to beetle in the shipped seed and nothing here followed. For eight days the project's primary change-detector for the RACE ran a snail on a track the product runs with a beetle, so one of its ten tracks did not cover the shipped race at all while the combined hash carried on looking authoritative. THE PREMISE WAS REPAIRED, NOT THE PAIR: swapping snail for beetle would have reproduced the defect the next time a default moves, so the instrument now READS each track's defaultRacerTypeId from the shipped seed on every run, and throws rather than substituting one if a seed lacks it. The track ORDER stays fixed and listed, because it feeds the combined hash and a track added to the seeds must not silently join the instrument.\n\n\u2605 THE MOVEMENT IS DEMONSTRATED, NOT ARGUED. Minting this value on trust would spend exactly the protection it provides, so both controls were run:\n  CONTROL B \u2014 the OLD instrument on the NEW tree: bc01b74fd4f3cfc8, EQUAL TO THE RECORD IT REPLACES. Nothing but the instrument changed.\n  CONTROL C \u2014 the NEW instrument on the OLD tree: 8a1977187e9c99b4, EQUAL TO THE NEW VALUE. The other three repairs in this block are inert for this hash.\n  AND THE PER-TRACK CHECK, which is the conclusive one: of the ten per-track hashes, EXACTLY ONE MOVED. garden-path fc492a4f8a63 -> 09df6c0a2f35; city-circuit, dirt-oval, ice-track, luger-hill, mountainstreet, river-run, searound, seatrack and space-sprint are byte-identical.\n\nTHE OTHER TWO FINGERPRINTS DID NOT MOVE, and that is corroboration rather than an absence. CAMERA 152cf295c4c9ff54 and RENDER 485b73d527602a0e are unchanged because they never had this defect: they resolve each track's racer from the loaded track record rather than from a literal, so they were already racing the beetle. The two instruments that were already correct are exactly the two that hold.\n\nWHAT HE IS AGREEING TO: that this recorded value changes because the INSTRUMENT was wrong, not because the game did.",
      "date": "2026-09-02",
      "lastVerified": "2026-09-02 \u2014 RE-MINTED by FINGERPRINT-TRACK-DEFAULTS-1, an instrument correction and not a world change. Measured on the branch tip, which is the tree master receives. Controls run and reported in full above: the OLD instrument on the NEW tree returns the value being replaced, the NEW instrument on the OLD tree returns the value being written, and exactly one of the ten per-track hashes moved. CAMERA and RENDER were run in the same pass and are UNMOVED, because they already read each track's own default. 2026-08-25 \u2014 RE-MINTED by GARDEN-PATH-DEFAULTS-1 (was dc4647be0f55ebdb). MEASURED, not argued: `npm run verify`'s routing SKIPPED all four fingerprints for this change, because the instruments read `server/data/tracks` (gitignored runtime state) while the shipped change is in `server/seeds/tracks`, so routing cannot connect the two. All four were therefore run by hand. That routing gap is reported rather than worked around. 2026-08-22 \u2014 RE-RUN IN FULL for the ENDGAME-LAND-CLEAN-1 ship and UNMOVED. Run rather than argued from a closure statement: defaults.js IS inside this instrument's declared closure and this ship genuinely edits it \u2014 it adds two camera keys and ships both ON \u2014 so only a measurement can say the race is untouched. 2026-08-21 \u2014 RE-RUN IN FULL for the START-ONE-WINDOW-1 ship and UNMOVED. Run rather than argued from a closure statement: defaults.js IS inside this instrument's declared closure (36 files) and this ship genuinely edits it \u2014 it RETIRES a camera key and adds another \u2014 so only a measurement can say the race is untouched. 2026-08-18 \u2014 RE-RUN IN FULL on the ENDGAME-THRESHOLD-095 merge 740f605c and UNMOVED. Run rather than argued from a closure statement: defaults.js IS inside this instrument's declared closure (36 files) because the race's keys and the camera's share one file, so only the CONTENTS of the diff make this a camera-only change and only a measurement can say so. Previously 2026-08-17 \u2014 NOT re-run at SHIP-RUNIN-1, and that is the stronger answer rather than a skipped step: walking scripts/lib/routing.mjs closureOf from this instrument's declared reach entries gives 36 files, and NONE of the 17 files the merge puts on master is one of them. The ship's only production file is CameraDirector.js, which is outside this closure and inside CAMERA's and RENDER's. engine-reach was deliberately not leaned on: on a committed merge it compares the working tree and can say nothing about the merge. Previously 2026-08-12 \u2014 re-run in full on eea0acf2 (the RUNIN-1 merge) and UNMOVED. Run rather than argued from engine-reach: defaults.js IS in the engine hull and this merge genuinely edits it, and engine-reach --check compares the WORKING TREE, so on a tree whose merge is already committed it reports byte-identical and can say nothing about the merge. The two keys it adds are camera keys, which is why the race is untouched \u2014 but that is the conclusion the measurement supports, not the reason to skip it. Previously 2026-08-12 \u2014 re-run at CLEAN-STATE-1.",
      "supersededMintedBy": "GARDEN-PATH-DEFAULTS-1 \u2014 THE OWNER'S DECISION OF 2026-08-25: on `garden-path` the default racer becomes BEETLE (was snail, 0.3x -> 0.9x) and the default lap count becomes 2 (was 4). Two lines in `server/seeds/tracks/garden-path.json`, the one place that owns a track record \u2014 `server/data/**` is a gitignored runtime dir and `defaults.js` holds no per-track values. ALL FOUR VALUES MOVED, and all four had to: every instrument runs all TEN tracks AT TRACK DEFAULTS, so changing which racer one track defaults to changes the race, the director's view of it and the frames drawn from it. NOT YET HIS: minted on the BRANCH `feat/garden-path-defaults-1`, unmerged, because the eye-test is whether garden-path still FEELS like the long slow track he designed \u2014 both changes together cut it from 424 s to 71 s. `mintedOn` names the PARENT commit 8834d7fc; it must be corrected to the merge commit if and when he merges.",
      "supersededDate": "2026-08-25",
      "supersededOn": "8834d7fc",
      "supersededNote": "bc01b74fd4f3cfc8 was the value while this instrument raced a SNAIL on garden-path. It was minted on 8834d7fc by GARDEN-PATH-DEFAULTS-1 and stood from 2026-08-25 to 2026-09-02. It is not a wrong record of a race \u2014 it is an honest record of the wrong race: nine of its ten tracks are byte-identical to the value replacing it, and the tenth ran a racer the product had stopped using. Anything compared against it on garden-path was compared against a race the game does not play."
    },
    "camera": {
      "value": "75aef5cd474c54e5",
      "covers": "the DIRECTOR's decisions: state, phase, anchor, zoom, offsets, camT, targets \u2014 INCLUDING the ending the director composes after the last crossing (CAMERA-ENDING-WINDOW-1)",
      "reproduce": "node scripts/camera-fingerprint.mjs --quiet",
      "mintedOn": "37a764b1",
      "mintedBy": "MINT-CAMERA-1 \u2014 THE OWNER'S DECISION OF 2026-09-08: MINT. AN INSTRUMENT CORRECTION, NOT A CAMERA CHANGE. The recorded value moves because the INSTRUMENT was describing a picture the product does not draw; the product itself is not changed by this branch.\n\n\u2605 THE PRODUCT WAS NEVER WRONG AND IS NOT CHANGED. `client/src/modules/camera/CameraDirector.js` is UNTOUCHED by this branch \u2014 established by diff against the merge-base, not remembered: the only files the branch changes under `client/src/modules/camera/` are twelve test files, each gaining a three-line environment header from SUITE-ENV-SPLIT-1. The director's decision code is byte-identical to master's. The two non-test product files the branch does touch are `modules/raceParams.js` (NEW \u2014 the ONE-HOME-RACE-PARAMS-1 extraction of the sprite-geometry derivation) and `RaceScreen/index.jsx` (which now calls it instead of carrying its own copy of the same arithmetic). That refactor is behaviour-neutral, and it is MEASURED rather than argued: world, world-off and render were run in this same pass and are byte-identical to their records.\n\nWHAT WAS WRONG. `scripts/camera-fingerprint.mjs` handed the director `isOutcomePhase: false` as a hard-coded literal, so every camera hash ever taken was measured with the race plan's OUTCOME window PERMANENTLY SHUT. The browser does not do that: it derives the flag from the plan at `RaceScreen/index.jsx:1271` as `racePlanController.getPhase(physicsTs, st.raceProgress) === 'OUTCOME'`, stores it on the diagnostics ref, and reads it back into the director's `raceState` at `RaceScreen/index.jsx:1492`. The instrument now makes the same call with the same two arguments.\n\n\u2605 THE BROWSER'S DERIVATION WAS ESTABLISHED CORRECT AT SOURCE BEFORE THIS WAS MINTED (OUTCOME-WINDOW-2), because a fix on the wrong side would have been enshrined here. Three checks, all with addresses. ARGUMENT ORDER: the signature is `getPhase(elapsedMs, phaseProgress)` (`racePlanner.js:524`) and the browser passes `(physicsTs, st.raceProgress)` \u2014 ms first, fraction second. THRESHOLD: the fraction path tests `corrStartFrac`/`corrEndFrac`, derived at `racePlanner.js:433-438` from the SAME `plan._phases` the millisecond path uses, so there is no second copy of the boundary to drift. CLOCK: the guard is `phaseProgress != null`, not a truthiness test, so a progress of exactly 0 does not fall through to the legacy path \u2014 and the plan's own physics runs on the identical call with the identical arguments at `raceCore.js:528`, whose `update()` computes `getPhase(elapsedMs, phaseProgress) !== 'OUTCOME'` at `racePlanner.js:557`. The camera is therefore reading the very predicate the plan steers by. It is the harness that was wrong, not the browser.\n\nWHERE THE FLAG CAN ACT, AND IT IS NARROW. `isOutcomePhase` has exactly ONE behavioural consumer \u2014 `CameraDirector.js:1717` \u2014 where it is OR'd with `leaderProgress > outcomePhaseThreshold` to admit COMEBACK_ZOOM to the candidate pool; the only other mention (`:914`) is diagnostics. So the flag can only change anything BELOW the internal threshold, in the band between the plan's OUTCOME start (`choreoOutcomeStart`, an owner-owned DevScreen slider) and that threshold \u2014 measured at 8.7 to 13.9 seconds depending on the track.\n\nWHICH FOUR TRACKS MOVED AND WHY THOSE FOUR. Ten tracks were run in both arms, frame by frame, recording state, anchor, zoom and both offsets. SIX SHOW ZERO DIFFERING FRAMES \u2014 garden-path, luger-hill, mountainstreet, river-run, searound and seatrack \u2014 so the blast radius is exactly four, independently reproduced rather than inherited from the moved hash. On dirt-oval, ice-track and space-sprint the game takes an 8.0 s COMEBACK_ZOOM that the old instrument could not see, entering at leader progress 0.639, 0.683 and 0.705 respectively \u2014 all inside the band \u2014 and displacing exactly one other state. On city-circuit NO comeback shot is taken in either arm: a comeback candidate enters the pool and loses the weighted pick, and because `_weightedRandomPick` (`CameraDirector.js:730`) draws one value only when the pool holds two or more candidates, admitting a losing candidate both re-normalises the choice and displaces the random stream. Its first differing frame is 1.5 s AFTER the band closes, and its visible consequence is a re-rolled closing stretch at progress 0.879-0.970. THOSE ARE TWO DIFFERENT PICTURES and are recorded as such.\n\nIT IS A PICTURE, NOT A HASH. `renderRaceFrame.js:153-158` applies `ctx.translate(offsetX, offsetY)` BEFORE `ctx.scale(zoom)`, so those offsets are SCREEN pixels on a 1280x720 canvas. The longest single episodes are 7.4 s on ice-track (COMEBACK_ZOOM on #32 where the old arm held LEAD_CHANGE on #0, peak offset delta 4023 px), 5.4 s on dirt-oval (4187 px) and 4.5 s then 3.5 s on space-sprint (up to 7091 px); city-circuit's three are 2.0-2.6 s with a peak of 4514 px. A 4000 px delta is three canvas widths, and the camera holds a DIFFERENT anchor racer for 6.8 to 12.3 s per race. Reported honestly against that: city-circuit's first two divergences are label-only \u2014 LEADER_ZOOM against LEAD_CHANGE on the SAME anchor, offset delta at most 10 px and zoom delta 0.00 \u2014 the same picture under a different name. For calibration, luger-hill takes COMEBACK_ZOOM for 264 frames in BOTH arms: the shot is not new to the game, the band only makes it reachable earlier.\n\n\u2605 EVERY CAMERA FIGURE TAKEN BEFORE THIS MINT DESCRIBES THE OLD, BLIND PICTURE ON CITY-CIRCUIT, DIRT-OVAL, ICE-TRACK AND SPACE-SPRINT, AND IS NOT COMPARABLE ACROSS THIS MINT. That covers every camera hash, every per-track camera hash and every camera-instrument statistic on those four tracks, whatever report it appears in. On the other six tracks the arms are byte-identical, so figures there carry across unchanged. This is stated as a rule rather than as a list because the instrument is old and the figures taken through it are many.\n\nWHAT HE IS AGREEING TO: that this recorded value changes because the INSTRUMENT was blind, not because the camera changed.",
      "date": "2026-09-08",
      "lastVerified": "2026-09-08 \u2014 RE-MINTED by MINT-CAMERA-1 (was 152cf295c4c9ff54, AIM-ROOM-SHIP-1's value). The value was RE-MEASURED on this tree before the record was touched and reproduced the decided value exactly: `node scripts/camera-fingerprint.mjs` -> 75aef5cd474c54e5, ten of ten tracks contributing FINISHED frames. THE OTHER THREE ROLES WERE RUN IN THE SAME PASS AND ARE UNMOVED, each confirming against the record with its own `--check`: world 8a1977187e9c99b4, world-off aa09ed97a3a32689, render 74946ddbeca517a9. That is what an instrument-only correction must look like, and it was run rather than argued \u2014 the branch does touch `RaceScreen/index.jsx` and adds `modules/raceParams.js`, so only a measurement can say the race and the drawing are untouched. `CameraDirector.js` is untouched by the branch, established by diff against the merge-base. RECORDED AND NOT FIXED: `scripts/render-fingerprint.mjs:584` still carries the same `isOutcomePhase: false` literal; measured both ways in this pass, fixing it would move the render record 74946ddbeca517a9 -> 40b2de6fcc5bafd8. That is a second mint and a second decision, and it is deliberately NOT taken here. 2026-09-02 \u2014 RE-MINTED by AIM-ROOM-SHIP-1 / AIM-ROOM-COMBINED-1 (was ff3072e95001083c / c5a9aadeaf7a1d07, COMPANY-HEADCOUNT-1's values). Measured on the BRANCH TIP, which IS the tree master receives: the branch was caught up with master first (THE SHIP ORDER step 1), so its tree and the merged tree are the same. WORLD and WORLD-OFF were run in the SAME pass and came back byte-identical to the record \u2014 only camera and render differ, which is what a camera-only change must look like, and the ten-track frame counts being identical between arms says the same thing from the other direction. 2026-09-02 \u2014 RE-MINTED by COMPANY-HEADCOUNT-1. Confirmed on the MERGED tree at 9328886b BEFORE this record was written, and both matched what was measured on the branch tip exactly (camera ff3072e95001083c, render c5a9aadeaf7a1d07), so nothing moved in the merge itself. WORLD and WORLD-OFF were run in the SAME pass on the same merged tree and came back byte-identical to their records \u2014 which is what a camera-only change must look like, and it was run rather than argued. `engine-reach --check` reports both changed paths OUTSIDE the engine hull, and the ten-track frame counts are identical between arms, so the race is untouched by construction and by measurement. 2026-08-26 \u2014 RE-MINTED by LEADER-LATERAL-BUILD-1 (was 341060f6's value). Measured on the branch tip, which IS the tree master receives because origin/master was already an ancestor of the branch \u2014 THE SHIP ORDER's step 1. WORLD and WORLD-OFF were run in the SAME pass and came back byte-identical to the record; only camera and render differ, which is what a camera-only change must look like. The owner's permission was for THESE TWO VALUES AND NO OTHERS, and the write was done surgically so that neither world hash appears in the diff at all. `mintedOn` was provisional and was corrected to the merge commit 334525df after CI went green for it, per THE SHIP ORDER step 11.",
      "supersededMintedBy": "AIM-ROOM-SHIP-1 / AIM-ROOM-COMBINED-1 \u2014 THE OWNER JUDGED CANDIDATE B ON PRODUCTION BUILDS OF `feat/aim-levers-1` ON 2026-09-01 AND 2026-09-02 AND ACCEPTED THE PICTURE. `leaderAimRoomFloorPx` default 0 -> 360, with the source-level wiring repair that makes `CameraDirector._anchorScreen` the one way the director obtains an aim. Its full note is in the record's history and in reports/evolution/; it is summarised here because this file keeps one superseded generation.",
      "supersededDate": "2026-09-02",
      "supersededOn": "73053d25",
      "supersededNote": "152cf295c4c9ff54 was AIM-ROOM-SHIP-1 / AIM-ROOM-COMBINED-1's value, minted on 73053d25 and accepted on 2026-09-02. It is the last state of this hash measured through an instrument that held the race plan's OUTCOME window permanently SHUT. It is not a wrong record of the director's decisions \u2014 it is an honest record of a director being asked the wrong question: on six of the ten tracks it is byte-identical to the value replacing it, and on the other four it describes a stretch of the race in which the game takes a COMEBACK shot that the instrument could not see."
    },
    "render": {
      "value": "74946ddbeca517a9",
      "covers": "the DRAW CALL SEQUENCE: sprite placement, text, styles, transforms, layer order",
      "reproduce": "node scripts/render-fingerprint.mjs --quiet",
      "mintedOn": "c8d988e4",
      "mintedBy": "MINT-RENDER-1 \u2014 THE OWNER ORDERED THIS MINT ON 2026-09-05, on the finding that the instrument had been hashing a frame the game does not draw. No drawing code, default or threshold changed; what changed is what the harness hands the renderer.\n\n\u2605 WHY IT MOVED. `render-fingerprint.mjs` built the frame's `camera` as a HAND-WRITTEN LITERAL with three members \u2014 `hudState`, `comebackLockedRacerIndex`, `detectBattleGroup` \u2014 while `client/src/screens/RaceScreen/frameCameraInputs.js` declares FIVE fields plus that method. So `state`, `anchorRacerIndex` and `runInArrived` were `undefined` on every frame the instrument drew, and TWO of those three are read by the very draw path it measures: `renderRaceFrame.js:212` \u2014 `let focusRacerIndex = camera?.anchorRacerIndex ?? null` \u2014 and `renderRaceFrame.js:220` \u2014 `const namesFromArrival = !!camera?.runInArrived`. Both verified at source on 2026-09-05. THE OLD HASH THEREFORE DESCRIBES FRAMES DRAWN WITH THE LEADER FALLBACK AND WITH LABELS SHOWING NUMBERS, while the game draws the anchored subject and NAMES. **Every render-hash comparison older than this mint describes that picture and is NOT COMPARABLE across it.** The repair is one line, `camera: frameCameraInputs(cd)`, so the field list is imported rather than retyped and a sixth field would reach the instrument with no edit.\n\nWHICH FIELD MOVED IT: exactly one. Bisected in INVISIBLE-FOUR-1 by adding each absent field alone, against a control that reproduced the old value: control 733b3f100d6a819f; `+state` 733b3f100d6a819f (unmoved); `+anchorRacerIndex` 733b3f100d6a819f (unmoved); `+runInArrived` 74946ddbeca517a9 \u2014 the whole move, identical to all three together. `runInArrived` decides whether a label draws a NAME or a NUMBER, so it changes `fillText` arguments and therefore the call stream that IS this hash. `state` moving nothing agrees with a four-form search finding it read by no live drawing code.\n\n\u2605 THE LIMIT THAT REMAINS, and it belongs beside this value rather than only in the recorder: TEXT MEASUREMENT IS SYNTHETIC (`client/src/modules/parity/recordingContext.js:26-31`). `measureText` must return something and the name-tag layout consumes its width to decide which labels are drawn; a real browser's width depends on the installed font, so the recorder returns a deterministic synthetic width. THE CONSEQUENCE IN THE TERMS THAT MATTER: this fingerprint pins the TAG-LAYOUT RULE \u2014 same widths in, same labels out \u2014 and NOT the number of labels a real browser would fall back to numbers on for want of space. A change to the layout rule is CAUGHT. A change in font metrics, and the real name-versus-number count the owner sees on screen, are NOT. That is what the browser gate and his eye are for, and this mint does not narrow that gap \u2014 it only stops the harness from being wrong about which labels it asked for.\n\n\u2605 OBSERVED AND UNEXPLAINED: `anchorRacerIndex` IS read by live drawing code, at `renderRaceFrame.js:212`, and adding it alone moved this hash by NOTHING. The sampled frames are evidently insensitive to it. That is recorded as an observation and nothing more \u2014 it is not investigated here, and it is NOT called harmless.\n\nWHAT MUST NOT MOVE, AND DID NOT (R17). world 8a1977187e9c99b4, world-off aa09ed97a3a32689 and camera 152cf295c4c9ff54 were all re-measured on the merged tree and are unchanged, the two world roles confirming against the record with their own `--check`. Only the RENDER role moves, which is what a change to the instrument's camera inputs \u2014 and to nothing the race reads \u2014 has to mean.",
      "date": "2026-09-05",
      "lastVerified": "2026-09-05 \u2014 RE-MINTED by MINT-RENDER-1 (was 733b3f100d6a819f, BOARD-PORTRAIT-FIT-1's value). Re-measured on the CAUGHT-UP branch tip 01ea234c, whose tree is the tree master took at c8d988e4, and it reproduced 74946ddbeca517a9 exactly before anything was written to this record. The other three roles were re-measured in the same pass and are unmoved.",
      "supersededMintedBy": "BOARD-PORTRAIT-FIT-1 - THE OWNER JUDGED THE STARTERS BOARD ON A PRODUCTION BUILD OF `fix/board-portrait-fit-1` (5f5e8c27) ON 2026-09-04 AND ACCEPTED IT: the icons fit. This hash is that acceptance written down.\n\nWHAT MOVED, AND IT IS ONLY THE PICTURE. The board asked for its portraits with `displaySizeScale = portraitPx / displaySize`, which sizes ONE axis - the visible body's NARROW one. The other axis is not bounded by that at all; it follows the sprite's proportions, and every shipped racer type has `baseRotationOffset` = 90 degrees, which lays it ACROSS the screen at the number chip on its left. The board's own comment believed otherwise, saying the drawn portrait 'goes to ~26.3 px' - 26.3 px was only ever its height.\n\nMEASURED BOTH WAYS BEFORE IT WAS BUILT. By geometry, against the 40 px the column allows: 13 of 20 types overflow, from rocket +17.9 px and giraffe +17.2 down to koi +0.8. By INK, read off the PNGs at alpha >= 10: 14 of 20 put opaque pixels on the number, giraffe 161.5 screen px2 down to manta 0.4. THE BEETLE, which is how it was found, is SEVENTH at 42.7 px2 - the HORSE, the default racer on most tracks, puts 45% more ink on the number and overflows 4.5 times as far. The beetle's displaySize is 38, below the median of the twenty. It was never a large sprite; it was a column that assumed a body is roughly square, and thirteen bodies are not.\n\nTHE FIX IS THE ASSUMPTION, NOT A SPRITE. No artwork and no racer config was touched. `SpriteRacerType` gains `getBodyBox` (the drawn body's on-screen box, rotation included) and `getPortraitFitScale` (the scale that fits a box), and the fill/guard/scale arithmetic `_drawBody` held inline is extracted so the two cannot drift. The TYPE answers, because the body fill, the rotation and the silhouette scale are its own. All twenty now fit on both axes and none reaches the chip.\n\nWHAT MUST NOT MOVE, AND DID NOT (R17). world 8a1977187e9c99b4, world-off aa09ed97a3a32689 and camera 152cf295c4c9ff54 were all re-measured ON THE MERGED TREE and are unchanged. The narrow-axis contract that the RACE reads is untouched and a test holds it: body fill feeds row layout and contact braking, so a racer that shrank to fit a box would change who wins. `getPortraitFitScale` is UI-only and nothing on the track calls it.\n\nTHE COST IS RECORDED BECAUSE HE ACCEPTED IT KNOWINGLY: fitting the width makes long racers shorter - rocket and giraffe to 35% of their former height, horse 44%, duck unchanged. That is the column's geometry (20 px of room left of the portrait's centre, 14 px right), not the fix's doing; widening the column would cost the name.",
      "supersededDate": "2026-09-04",
      "supersededOn": "db1a1cc5",
      "supersededNote": "733b3f100d6a819f was BOARD-PORTRAIT-FIT-1's value, minted on db1a1cc5 and accepted on 2026-09-04. It is the last state of this hash before the instrument was given the frame camera the game actually draws with. IT DESCRIBES A PICTURE THE GAME DOES NOT DRAW, and every render-hash comparison at or before it describes that picture: the harness supplied three camera members where `frameCameraInputs.js` declares five plus a method, so `anchorRacerIndex` and `runInArrived` were undefined on every frame it hashed. Nothing about the race, the world or the drawing code differs between that value and this one; only what the instrument was told about the shot."
    },
    "world-off": {
      "value": "aa09ed97a3a32689",
      "covers": "the world with gap-reroll OFF \u2014 the pre-feature invariant that proves the shipped change is the only thing that moved",
      "reproduce": "node scripts/fingerprint-default.mjs off --gapRerollEnabled=false",
      "mintedOn": "fa553f50",
      "mintedBy": "FINGERPRINT-TRACK-DEFAULTS-1 \u2014 AN INSTRUMENT CORRECTION. THE OWNER GRANTED MINTING PERMISSION ON 2026-09-02 FOR THIS MINT AND NO OTHER, on the reasoning that the picture does not change \u2014 the instrument does \u2014 so there is nothing for his eye to judge.\n\n\u2605 THIS IS NOT A WORLD CHANGE, AND A READER IN SIX MONTHS MUST NOT CONCLUDE THAT IT IS. The simulation is untouched: the diff contains no engine file, no default, no config, and no seed. SHIP-CEREMONY's world-changing half \u2014 REBASELINE, the SIM lineage entry for a moved world, the golden re-pin \u2014 exists for the case where the RACE moved, and it does NOT apply here. What moved is which race the instrument runs.\n\nWHAT WAS WRONG. `scripts/fingerprint-default.mjs` carried a literal table of ten track/racer pairs under a comment reading '10 standard tracks x default racer'. That was true when written and false from 2026-08-25, when GARDEN-PATH-DEFAULTS-1 changed that track's defaultRacerTypeId from snail to beetle in the shipped seed and nothing here followed. For eight days the project's primary change-detector for the RACE ran a snail on a track the product runs with a beetle, so one of its ten tracks did not cover the shipped race at all while the combined hash carried on looking authoritative. THE PREMISE WAS REPAIRED, NOT THE PAIR: swapping snail for beetle would have reproduced the defect the next time a default moves, so the instrument now READS each track's defaultRacerTypeId from the shipped seed on every run, and throws rather than substituting one if a seed lacks it. The track ORDER stays fixed and listed, because it feeds the combined hash and a track added to the seeds must not silently join the instrument.\n\n\u2605 THE MOVEMENT IS DEMONSTRATED, NOT ARGUED. Minting this value on trust would spend exactly the protection it provides, so both controls were run:\n  CONTROL B \u2014 the OLD instrument on the NEW tree: bc01b74fd4f3cfc8, EQUAL TO THE RECORD IT REPLACES. Nothing but the instrument changed.\n  CONTROL C \u2014 the NEW instrument on the OLD tree: 8a1977187e9c99b4, EQUAL TO THE NEW VALUE. The other three repairs in this block are inert for this hash.\n  AND THE PER-TRACK CHECK, which is the conclusive one: of the ten per-track hashes, EXACTLY ONE MOVED. garden-path fc492a4f8a63 -> 09df6c0a2f35; city-circuit, dirt-oval, ice-track, luger-hill, mountainstreet, river-run, searound, seatrack and space-sprint are byte-identical.\n\nTHE OTHER TWO FINGERPRINTS DID NOT MOVE, and that is corroboration rather than an absence. CAMERA 152cf295c4c9ff54 and RENDER 485b73d527602a0e are unchanged because they never had this defect: they resolve each track's racer from the loaded track record rather than from a literal, so they were already racing the beetle. The two instruments that were already correct are exactly the two that hold.\n\nWHAT HE IS AGREEING TO: that this recorded value changes because the INSTRUMENT was wrong, not because the game did.",
      "date": "2026-09-02",
      "lastVerified": "2026-09-02 \u2014 RE-MINTED by FINGERPRINT-TRACK-DEFAULTS-1, an instrument correction and not a world change. Measured on the branch tip, which is the tree master receives. Controls run and reported in full above: the OLD instrument on the NEW tree returns the value being replaced, the NEW instrument on the OLD tree returns the value being written, and exactly one of the ten per-track hashes moved. CAMERA and RENDER were run in the same pass and are UNMOVED, because they already read each track's own default. 2026-08-25 \u2014 RE-MINTED by GARDEN-PATH-DEFAULTS-1 (was 854018ee5d3d83e1). MEASURED, not argued: `npm run verify`'s routing SKIPPED all four fingerprints for this change, because the instruments read `server/data/tracks` (gitignored runtime state) while the shipped change is in `server/seeds/tracks`, so routing cannot connect the two. All four were therefore run by hand. That routing gap is reported rather than worked around. 2026-08-22 \u2014 RE-RUN IN FULL for the ENDGAME-LAND-CLEAN-1 ship and UNMOVED, for the same reason as WORLD above. 2026-08-21 \u2014 RE-RUN IN FULL for the START-ONE-WINDOW-1 ship and UNMOVED. Run rather than argued from a closure statement: defaults.js IS inside this instrument's declared closure (36 files) and this ship genuinely edits it \u2014 it RETIRES a camera key and adds another \u2014 so only a measurement can say the race is untouched. 2026-08-18 \u2014 RE-RUN IN FULL on the ENDGAME-THRESHOLD-095 merge 740f605c and UNMOVED. Run rather than argued from a closure statement: defaults.js IS inside this instrument's declared closure (36 files) because the race's keys and the camera's share one file, so only the CONTENTS of the diff make this a camera-only change and only a measurement can say so. Previously 2026-08-17 \u2014 NOT re-run at SHIP-RUNIN-1, for the same closure reason as the ON world above: this is the same instrument under a flag, so it has the same 36-file closure and the same answer. Previously 2026-08-09 \u2014 re-run at SHIP-ROUTING and UNCHANGED, so the pre-feature invariant holds either side of this ship too. SHIP-THREE did not re-run it: nothing in that ship touches the engine (engine-reach clears every changed path except defaults.js, whose only moved key is read by the camera), and the ON world is unchanged, so the OFF arm has no question to answer. Previously re-run at the SHIP-THE-LINE merge and UNCHANGED, so the pre-feature invariant still holds either side of that ship. Previously 2026-08-06 \u2014 RESOLVED and VERIFIED by running it. This was UNKNOWN in ONE-TRUTH-1. docs/SIM.md's lineage table names the flag; the first command written here OMITTED THE LABEL argument, argv[2] swallowed the flag as a label, and the run returned the ON value while printing 'default config'. The label is mandatory. With it, this value reproduces exactly. Re-run at RENDER-SAMPLER-CEREMONY on 0308f7b9 and UNCHANGED, measured rather than argued because the ON world was measured too.",
      "supersededMintedBy": "GARDEN-PATH-DEFAULTS-1 \u2014 THE OWNER'S DECISION OF 2026-08-25: on `garden-path` the default racer becomes BEETLE (was snail, 0.3x -> 0.9x) and the default lap count becomes 2 (was 4). Two lines in `server/seeds/tracks/garden-path.json`, the one place that owns a track record \u2014 `server/data/**` is a gitignored runtime dir and `defaults.js` holds no per-track values. ALL FOUR VALUES MOVED, and all four had to: every instrument runs all TEN tracks AT TRACK DEFAULTS, so changing which racer one track defaults to changes the race, the director's view of it and the frames drawn from it. NOT YET HIS: minted on the BRANCH `feat/garden-path-defaults-1`, unmerged, because the eye-test is whether garden-path still FEELS like the long slow track he designed \u2014 both changes together cut it from 424 s to 71 s. `mintedOn` names the PARENT commit 8834d7fc; it must be corrected to the merge commit if and when he merges.",
      "supersededDate": "2026-08-25",
      "supersededOn": "8834d7fc",
      "supersededNote": "daf78ff18eca83c6 was the value while this instrument raced a SNAIL on garden-path. It was minted on 8834d7fc by GARDEN-PATH-DEFAULTS-1 and stood from 2026-08-25 to 2026-09-02. It is not a wrong record of a race \u2014 it is an honest record of the wrong race: nine of its ten tracks are byte-identical to the value replacing it, and the tenth ran a racer the product had stopped using. Anything compared against it on garden-path was compared against a race the game does not play."
    }
  },
  "historyHomes": [
    "docs/TAGS.md",
    "reports/"
  ],
  "machineExceptions": [],
  "_machineExceptionsNote": [
    "Files that must contain a current fingerprint as a LITERAL because something reads it \u2014 a test,",
    "a baseline, a script. Listed BY NAME, never by pattern, each with its reason.",
    "",
    "The list is EMPTY, and that was measured rather than assumed: every tracked file was scanned and",
    "not one test, script or config consumes a fingerprint string. All 19 former copies were prose or",
    "comments meant for a human. If you are about to add one, make it read this file instead."
  ]
}
